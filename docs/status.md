# Implementation Status

Textual MLIR remains the complete development and testing entry point. The
experimental `.ox` frontend is a narrower typed source surface over the same
contracts; its exact coverage is the "Experimental `.ox` frontend" row below.
Bindings expose every declared choice their contract admits; a binding never
narrows a contract silently. Dialect and source contracts may change while
the numeric model is stabilized.

Rows in this matrix are backed at different evidence strengths, and the
wording marks which: *exhaustive* means every input of a finite domain was
executed (E1); *differential* / *object+C gate* means discriminating
witnesses across independent implementations (E2); *certified* /
*proof-gated* means a static sufficient condition consumed fail-closed with
counterexamples for the refused side (E3); explicitly sampled rows with an
error bound are bounded-empirical (E4). The levels are defined in
[research-claims.md](research-claims.md) and are deliberately not
interchangeable.

| Capability | Status |
| --- | --- |
| `ondrix`, `ondsp`, and `ortumcore` dialects | Implemented; contracts remain experimental |
| Typed conversion patterns and accumulator type conversion | Implemented |
| Generic scalar lowering | Signed-Q15 full product with signed frac30 accumulator widths of at least 32 bits and signed-Q31 full/raw-high accumulator operations implemented; ordered rank-1 Q15/Q31 memref reductions implemented; every accumulator is carried in the next legal machine width (i40 and i48 travel as i64), a saturating carrier always holding the sign-extended storage value and a wrapping carrier any value congruent to it modulo the storage width, folded back to the storage only at export or at a storage-bits cast, so no per-update sign-extension of an illegal width reaches LLVM, and a wrapping update is formed in the carrier itself (a wrapping update whose carrier is wider than the exact product also multiplies in the carrier, scalar or multi-lane; a runtime multiplicand against runtime lanes is widened before its broadcast to the carrier only under the declared `widening-multiply-low-halves` target capability, x86 `pmuldq`, and otherwise stays at the exact product width so a NEON `smull`/`smlal` selection sees its sign-extended lanes) while a saturating one is formed in the exact update width; a single-lane export whose storage is narrower than its carrier rounds `nearest_ties_positive` as one add-half and one shift, since the add cannot leave the carrier, while a storage as wide as its carrier, and every multi-lane export, keeps the remainder-compare form (on x86 the add-half form legalizes worse across lanes); from a supported signed frac30 accumulator the export boundary lowers a signed Q15 destination, any signed i32 destination fraction the verifier admits, and the identity signed i64/frac30 reading, while a signed i64 accumulator at any fraction up to 62 exports to any signed i32 destination fraction the verifier admits or to the identity i64 reading at its own fraction, which is what carries the Q31 matmul and rms bufferizations at their extent-derived accumulator fractions; every export is a value-preserving format conversion, and value-changing scalings belong to the declared arithmetic `round_shift`; rank-1 f32 reduction is partial |
| Result-buffer forwarding (`forward-ondrix-result-buffers`) | Implemented: after `buffer-results-to-out-params`, a function whose result allocation is copied whole into an otherwise untouched out-parameter writes into the out-parameter directly, erasing the allocation, the copy and the deallocation; benchmarks that give Ondrix objects the caller-buffer ABI of their C baselines use it |
| Floating-point contract policy | Every f32 call site names one of three declared evaluation policies. `off` and `fma` are exact contracts, pinned bit-for-bit against independent references; `fast` names a set of two permitted rewrites and is spent by the compiler rather than handed on, so the emitted operations carry no fast-math flags at all. One transform exploits it, `vectorize-ondsp-fp-fast-memref-reduce`, and its schedule is gated at the object level. `docs/f32-contract-evidence.md` is the normative statement of the permissions, the admission rule, and what each route spends; this row reports maturity only and is deliberately not a second copy of it. |
| Canonical pipeline and automatic scheduling | `-ondrix-default-pipeline` (and `ondrix-compile --emit=llvm` from `.ox` source) runs the whole flow with one flag; the stage list, the schedule-selection rules, and the width parameter are in [Canonical pipeline and scheduling](#canonical-pipeline-and-scheduling) below |
| Generic Vector CPU lowering | Automatic unit-stride Q15/Q31 chunking, ordered saturating updates, and exact-modulo wrapping reduction implemented (one machine vector of i64 lane sums carried to the vector loop exit and folded once, since wrapping updates commute modulo the storage width; a sequence reduced against itself with 32-bit full products folds term pairs in i32 and widens them unsigned, because two nonnegative squares reach at most 2^31 and the unsigned reading holds that carry, under the declared `multiply-add-adjacent-pairs` target capability, x86 `pmaddwd`, and otherwise widens each product on its own so a NEON `smull`/`saddw` selection pays no permutes); the horizontal wrapping path admits signed frac30 Q15 accumulators wider than i40 up to i64, since exact-modulo reassociation legality is width-independent up to the i64 lane carrier; a genuinely wrapping i48 reduction object gate pins horizontal, ordered, and an exact mod-2^48 reference bit-identical on cases whose ordered prefixes leave the accumulator range, including an exact full-turn return to zero; the constant-coefficient prefix-range proof also certifies two narrower carriers, the adjacent-pair sum and the whole chunk sum in i32, recorded in the trace as `pair_term_width` and `implementation_term_width` (schema 2), so a certified chunk pre-adds its products in pairs (the shape x86 selects a packed multiply-add for) and widens only the pair sums; the route admits a reversed constant view (a convolution's flipped kernel), certifying the coefficients in read order and undoing the reversal in the load, and admits a wrapping accumulator, whose update mode the certificate makes coincide with the saturating one; the same proof also discharges the accumulator's update guard, since authorization already requires every prefix of the emitted schedule to fit the accumulator storage, so a certified schedule whose accumulator reaches only `acc_export` carries the wrapping accumulator of the declared storage, frac and signedness, while a refused reduction and an escaping accumulator keep their saturating updates |
| Output batching width | Every output batcher (`fir_decimate`/`fir_filter`/`conv1d` sliding windows, matrix columns, unrolled constant DCT rows) takes one declared lane count per target -- the register width -- and picks the widest block the output count fills: the sliding-window batcher on a ladder of `chunk-multiple` machine vectors (the default pipeline passes 2: a four-vector block of per-lane i64 accumulators spills a 128-bit register file) down to one and then halves, the column and constant-row batchers by halves; no per-kernel width is chosen, and a kernel whose shortest reduction is narrower than the register keeps its ordered schedule, which the runner records |
| Algorithm transforms | Opt-in specialization for static rank-1 FIR samples using immutable constant memref globals or proven complete unit-stride views: zero-tap elimination, exact-modulo symmetric full-product pairing, and symmetric saturating pairing when complete prefix-range analysis proves both schedules safe |
| Proof audit | Experimental optional JSON traces record constant saturating chunk-reassociation evidence; a read-only replay pass rederives immutable coefficient facts and the subject-bound prefix plan from the original IR, and rejects changes to proof-relevant semantics or trace data; traces are audit artifacts, not legality authority, an independent validator, or a stable certificate format |
| Full-output FIR | Experimental tensor-only destination-style `ondrix.fir_filter` for valid and zero-padded full boundaries; both expose output-axis tiling, with full tiles carrying an explicit global output origin; full lowering uses guarded scalar edges plus a Q15/Q31 Vectorized interior; immutable Q15/Q31 coefficients can activate prefix-proof-authorized horizontal Vector reduction after bufferization; the opt-in `vectorize-ondsp-fp-filter-outputs` pass batches W independent valid-boundary f32 outputs into vector lanes under the exact contracts — each lane runs its declared per-output event graph verbatim (off: separate ordered products and sums; fma: one fused event per tap), so the authorization is structural per-lane event-graph identity, needs no arithmetic law of the element domain, admits a fast site on the same structural ground, spending F only where a declared vector fused multiply-add selects the fused member, reads no element the ordered schedule did not, keeps a loop-form tap body valid for any static coefficient count (where unroll-plus-SLP compilers stop at their unroll threshold), and discharges the same statically-refused aliasing obligation as the fixed decimate batching with the `.ox` ABI's declared entry disjointness as the only residual; ordered f32, dynamic shape guards, `K > N`, alias-safe tiled/untiled AOT execution, and direct bufferization are covered; same boundaries, public buffer semantics, fusion, and target-aware selection remain open |
| FIR decimation | Experimental tensor-only `ondrix.fir_decimate` defines valid phase-zero decimation with static factor, increasing tap order, and result length `floor((N-K)/D)+1`; signed-Q15/full with signed frac30 accumulators of at least 32 bits has generic scalar and ordered fixed-Vector lowering; factor-two i40 textual MLIR and portable i34 `.ox` profiles reach object+C differential execution; the opt-in `vectorize-ondsp-fixed-decimate-outputs` pass batches W outputs into a lanes-W accumulator (per-lane tap order unchanged, so the batching needs no arithmetic reassociation proof even for saturating accumulators — the order-preserving complement of the proof-gated horizontal routes — while the memory-schedule disjointness it does require is discharged separately, by static base-storage refusal — with non-entry block arguments, whose incoming operands are in-function facts no caller owns, refused as opaque — plus a documented caller-side ABI residual over function entry arguments and globals only), firing on the static identity-layout window loop shape whose input, coefficient, and output storage it can see to be disjoint -- factor two here, and since 2026-09-03 the unit-stride windows of `fir_filter` and `conv1d` as well (a reversed constant kernel is read one scalar per tap; a saturating accumulator whose constant coefficients certify that no ordered prefix reaches the rail runs its lanes wrapping, and the widest power-of-two tap group whose product sums are certified to fit i32 accumulates in an i32 lane group that joins the declared accumulator as one multi-lane `acc_add_term`) -- with an ordered scalar remainder, gated by a differential object test whose i34 profile reaches its saturation rail (8 x 2^30 = 2^33) inside the vector body and, at seventeen taps, exports the clamped trajectory as a non-saturating value the unclamped i40 profile disagrees with; the f32 profile carries the same index relation with the accumulator machinery replaced by its declared contract, reaches direct bufferization, and admits the fast horizontal reduction because a decimated window is still unit stride; a Q31 profile pairs the signed Q31 numeric with the i64/frac62 accumulator the Q31 reduction family declares and carries the tap sum by that accumulator's declared per-update overflow mode rather than a narrowed product, since no exact accumulator exists for K products of 62 bits — which is why automatic accumulation stays Q15-only and a Q31 `.ox` resampling site spells its accumulator/rounding/overflow triple; reassociating cross-output polyphase transforms, nonzero phase, and target consumers remain unsupported |
| FIR interpolation | Experimental tensor-only `ondrix.fir_interpolate` defines factor-two phase-zero interpolation as the complete convolution of a zero-inserted input, with result length `(N-1)*2+K` and increasing coefficient update order; signed-Q15/full i40 textual MLIR and portable i33 `.ox` profiles reach generic scalar object+C differential execution, as does the f32 profile under both exact contracts; skipping the inserted-zero terms is a declared f32 event rather than a derivable rewrite (the skipping and materializing programs agree on every finite input and part company at an infinite coefficient, which the object gate witnesses); a Q31 profile shares the resampling contract above, with the same declared-overflow tap sum and the same explicit `.ox` triple; stateful block forms, polyphase scheduling, Vector, and target consumers remain unsupported |
| Convolution/correlation | Experimental tensor-only `ondrix.conv1d` preserves distinct valid one-dimensional convolution and correlation identities with explicit kernel orientation and increasing-window update order; Q15 and f32 generic tensor lowering, direct bufferization, dynamic shape guards, LLVM object+C differential execution, unit-stride correlation Vector reuse, and reverse-stride convolution scalar fallback are covered; the `.ox` frontend exposes matching valid tensor forms; full/same padding, stride, dilation, multidimensional/grouped forms, complex conjugation, and target selection remain unsupported |
| Streaming FIR | Experimental tensor-only `ondrix.fir_stream` with explicit chronological `K-1` history; ordered Q15/Q31/f32 generic scalar lowering, empty and short chunks, `K=1`, dynamic shape guards, multi-chunk state equivalence, and object+C execution are covered; an opt-in materialized concat decomposition reuses the existing Q15/Q31 Vector path; `.ox` exposes one dynamic-chunk/static-tap profile with two tensor results at both widths — Q15 with inferred exact accumulation and the export-default `nearest_ties_positive` saturating export when the policy is omitted, or a spelled `accumulator=exact[40,...]`/`rounding=`/`overflow=` triple; Q31 with the triple required, because two Q31 products already leave i64 and no inferred width makes the update mode vacuous — and the Q31 object gate's corpus reaches the i64 rail, which a wrap mutant fails; zero-copy buffer scheduling, broader source profiles, reset/resampling policies, circular-buffer capabilities, and stable ABI remain open |
| Recursive filters | Experimental tensor-only `ondrix.sos_filter_tdf2` defines an explicit f32 transposed-direct-form-II cascade; experimental `ondrix.sos_filter_df2_fixed` separately defines a signed uniform-Q DF-II cascade for Q15/full+i40/frac30 and Q31/full+i64/frac62 with independent state/output export policies; both have dynamic section guards, split/empty-chunk state equivalence, generic lowering (a static cascade of at most eight sections extracts its coefficients once ahead of the sample loop and carries the delay elements as loop scalars), and object+C differential execution; `.ox` exposes one static-section DF-II profile at Q15 (i40; an omitted policy takes the vacuous-wrap default) and at Q31 (i64; every policy spelled, since three Q31 products leave i64), each with explicit accumulator and state/output policies plus whole/split execution against a corpus that reaches the i64 rail, and the static-section f32 TDF-II profile under `contract=off`/`fma` with its own whole/split/empty object gate; nonuniform fixed scaling, raw-high products, Vector lowering, public buffer semantics, target selection, broader source profiles, and stable state ABI remain unsupported |
| Packed Q15 butterfly | One closed scalar/fixed-Vector `i32` profile is executable: signed-Q15 complex values use imag-high/real-low packing, exact i33/Q30 cross-products, explicit nearest-even saturating product and output requantization, generic elementwise lowering, and object+C differential execution; the `ondsp.cx_butterfly` contract additionally admits the packed target inventory (`toward_negative` or `nearest_ties_positive` rounding, wrap or saturating narrowing, output shift 0 or 1) under the same generic lowering, and, on the scalar Q15 inventory profile only, a `cross` combine variant forming `a -+ j*t` from the same requantized product (the packed target's free `-j` leg; not equivalent to a plain butterfly with twiddle `-j*w`, and fail-closed elsewhere) plus exact `unit` and `unit_cross` variants (no product stage at all, `t` IS `b`, twiddle ignored, same gating), and the opt-in `convert-ondsp-cx-butterfly-to-ortumcore` selects scalar inventory butterflies of either variant with constant or compile-time-table twiddles onto the OrtumCore packed pair through exactly conjugated twiddle constants and once-materialized conjugated tables (an imaginary -32768 anywhere in the constant or table, runtime twiddles, and the nearest_even default stay generic, fail-closed), gated by a two-leg object+C differential; the algorithm-level FFT contracts additionally admit the gated Q15 inventory and the frozen packed-Q31 profile (their own rows), while broader complex profiles plus frontend butterfly syntax remain unsupported |
| Packed CFFT (Q15/Q31) | Experimental static power-of-two forward and inverse radix-2 DIT transforms for extents 4 through 1024 over matching `tensor<Nxi32>` values with natural input/output order, recursive even/odd stage decomposition, tie-guard-generated Q15 twiddle tables, explicit per-stage scaling, generic scalar lowering, and object+C differential execution (CI gates at 4/8/64); an opt-in fft-loops conversion mode lowers the same dataflow as bit-reversal and stage loops over in-memory twiddle tables, bit-identical per element and gated against the same references, collapsing the unrolled object footprint and compile cost (RFFT256 object 313 KB to 5 KB); the generic loop form walks each stage group-major at unit stride and, under `fft-loops-vector-width`, computes that many consecutive butterflies of every stage whose half length reaches it as one fixed-length vector (stages below it stay scalar), which changes no update order because a stage's butterflies are independent; an opt-in conversion mode batches independent combine-stage butterflies into fixed-length Vector arithmetic without crossing stage or requantization boundaries, and agrees bit-for-bit with scalar/reference execution; the two opt-in modes are mutually exclusive; inverse transforms use conjugated twiddles and the per-stage shifts provide `1/N` normalization; an independent opt-in scalar-finalizer mode consumes subject-bound legality plans for exact finite-precision `+1` and `-j` identities while other constants and runtime twiddles retain generic i33 products; the `.ox` frontend exposes the bounded four-/eight-point profiles as `cfft` and `icfft` over `tensor[complex_q15,4|8]` and permits unary CFFT/ICFFT nesting while preserving each intermediate quantization boundary; a packed-Q31 profile (`tensor<Nxi64>`, imag high / real low, extents 4 through 64) reuses the same recursion, and its transform equation is the Q31 scalar target's (re-frozen 2026-08-18): `#ondsp.product<high_raw>` narrows every cross term to the arithmetic high half of its own product BEFORE the combine, so each term carries its own floor and two independent floors are not one floor of their sum; the exact combine and the `pre_shift_left = 1` that returns the term to the component fractional position both fit an i34 carrier, and both boundaries declare `toward_negative` with saturation, the only rounding and overflow the target's scaled add/sub performs — so no i128 appears anywhere in the transform. The transform-level cfft/rfft/irfft verifiers refuse any other Q31 product selection; the full-product selection stays admitted on the operation, where its exact i128 cross-product carrier is required by the `ondsp.cx_butterfly` contract: arbitrary SSA twiddles let `br*wi + bi*wr` reach exactly `2^63`, and the operation-level rail gate (scalar and two-lane Vector, all four sign×parity shift-31 tie cells) refutes a WRAPPING i64 carrier at that corner; i128 is the exact generic carrier choice rather than the only legal one, since `2^63` is the single value past i64 and an overflow-aware saturating-i64 sum is observably equivalent under that boundary (executed as a mutation experiment: wrapping i64 fails the gate with the predicted component swap, saturating i64 passes the entire corpus) and would need its own equivalence proof; the same operation gate runs the raw-high selection over the whole corpus against its own reference and refuses to run unless its two named cases separate the per-term floors from a fused floor of the combined sum. The transform's twiddles remain the frozen 50-digit-mpmath Q31 tables (in-compiler binary64 evaluation cannot prove correct rounding at the Q31 guard even though the actual margins are large; the generator's CI-run self-check reproduces the pinned Q15 stage-2/4/8 words and both complete extent-64 distinct word sets the compiler emits), with forward/inverse object+C differential gates at 4/8/16/64 against an independent reference and a bit-exact staged forward∘inverse composition whose nominal net scale is 1/N — not an identity — plus an empirical ±6-LSB consistency check on bounded inputs; the opt-in fft-loops mode follows the packed profile, so the Q31 profile takes it in the generic shape — an i64 twiddle table and reversal-table gather, the paired inventory shape being the 16-bit target's alone — and the same forward/inverse and composition gates run the loop pipeline against the same reference; the opt-in `convert-ondsp-cx-butterfly-to-ortumcore` selection reaches this profile by DECOMPOSING it rather than matching it — the OrtumCore surface has no direct packed-Q31 capability and needs none: the container halves come apart with plain arithmetic, each cross-term pair becomes one accumulator web of raw-high MAC steps (`ortumcore.q31_mac_add`/`q31_mac_sub`, sharing the Q15 family's accumulator and readout because the raw high half lands at that accumulator's own fractional position), the product scale's left shift becomes a saturating doubling of the shift-0 readout (equal to doubling the exact term sum: clamping before a monotone doubling that clamps again agrees with clamping after), and each stage leg becomes one `ortumcore.sat_shift_add`/`sat_shift_sub` carrying the declared output shift; the full-product profile stays generic there, and the emulated selection executes against the same independent reference as the scalar and loop legs; the opt-in Vector-batched conversion also follows the packed profile, batching the independent Q31 combine-stage butterflies over the i64 container with the same bit-identity gates, and the raw-high Vector butterfly carries its own operation-level lane gate; the `.ox` frontend binds the profile as `cfft`/`icfft` over `tensor[complex_q31, 4..64]` (power-of-two; the frozen policy is the only admitted stage spelling, and the binding reaches object execution against an independent raw-high reference through both the scalar and Vector-batched pipelines); a Q31 adapter backend remains unsupported; the Q15 `ondrix.cfft` contract additionally admits the packed target inventory per call site (`toward_negative`/`nearest_ties_positive`, wrap or saturating, both scale boundaries), the unrolled lowering propagates it into every stage butterfly, and under the inventory profile the transform equation is the target equation (re-frozen 2026-08-17): the size-2 and size-4 combines are the exact unit stages (no product stage, twiddle exactly one or exactly `-j`, only the per-stage output scaling), and every larger combine pairs stage legs j and j+H/2 on one shared requantized twiddle product with the second leg as the cross combine, so `-j` never multiplies anywhere; both the unrolled and fft-loops lowerings emit the staged form (the loop form fuses the two exact stages into one branch-free group loop that reads the input through a loop-carried `ondsp.bitrev_add` reversed-carry cursor — no separate permute loop and no reversal table — ahead of the dynamic quarter-turn-table stage loop, every body carrying one fixed variant with no in-body branch, and the packed selection maps each cursor step onto the `ortumcore.bitrev_add` walk through an exact top-aligned 32-bit composition; the Vector-batched mode does not apply to the inventory profile, whose conversion is always the paired scalar form), the opt-in packed selection then converts every stage butterfly with nothing left generic (unit legs as the bare packed butterfly, the exact unit cross through a plain-arithmetic halves swap), and a whole-transform four-leg object+C gate (unrolled and loop form, each scalar and packed-emulated) pins all four inventory combinations at eight points against an independent staged recursive reference; the `cfft`/`icfft` source bindings expose the stage policy as optional per-call `rounding=`/`overflow=` parameters (one pair names both scale boundaries, the gated inventory shape; `nearest_even` with wrap has no gate and is refused; omission keeps the closed default), all four inventory combinations carry the whole-transform gate; Vector-mode inventory profiles and other expression composition remain unsupported. An INTERLEAVED f32 profile gives `ComplexLayout::interleaved` its first executable contract: one complex value is two adjacent `f32` elements (real first), so a size-N transform maps `tensor<2Nxf32>` to itself for N in [4, 1024]. It declares NONE of `product`, `product_scale`, `output_scale` — the program contains no requantization boundary and the verifier refuses any of them — and its one contract axis is the format's own `contract` mode, spent on the two multiply-adds of each butterfly's complex product. Three statements the fixed-point profiles cannot make: the legs whose twiddle is exactly one (index 0) or exactly `-+j` (index n/4) contain NO product event, which is contract rather than strength reduction because `0.0 * x` is not the identity on an infinite or negatively signed operand; the inverse `1/N` is one final multiply, agreeing with `log2(N)` stage shifts bit for bit only while no intermediate leaves the format's range (a size-4 inverse of four `2^126` values returns an infinity where per-stage halving returns `2^126`); and conjugation is an exact sign flip where the packed profiles need a saturating select. Twiddles are the binary32 roundings of a binary64 evaluation with no tie guard, for the reason stated on the f32 DCT table, so the bits are pinned in a conversion test. The fft-loops lowering covers it and peels the exact phases into their own loops, staying bit-identical to the unrolled form; the Vector-batched mode does not. Six kernels are gated bit-exactly against a reference that walks the same event graph, on the convert-first route, the canonical pipeline, and the loop form, with a witness that `off` and `fma` must disagree; `.ox` binds it through a `complex_f32` element type whose declared extent is the transform size rather than the element count |
| Packed RFFT (Q15/Q31) | Experimental static power-of-two `ondrix.rfft`/`ondrix.irfft` profiles for real extents 8 through 1024 map real Q15 tensors to compact natural-order packed Hermitian bins and back; DC/Nyquist imaginary components are canonicalized to zero, inverse reconstruction uses saturating Q15 conjugation, and all full-length CFFT stage boundaries remain observable; generic scalar lowering and an opt-in fixed-Vector mode for independent packed-complex CFFT combine stages both reach LLVM object+C differential execution (CI gates at 8/16/64/256) and sequential finite-precision round trips; the opt-in fft-loops lowering additionally gates the maximum 1024-point extent in CI, which the unrolled form could not afford to compile — that pass option remains the standalone ablation and oracle interface, while `ondrix-compile --fft-loops` is the deployment-facing selection of the same shape and is recorded in the reproduction manifest under `declared_schedule_choices` rather than `declared_target_facts`, because no target description determines it (a module holding a size-8 transform at 4128 bytes unrolled and a size-64 one at 69352 has no correct global value); a separate static 32-point `ondrix.rfft_radix4_split` contract fixes an explicit radix-4 split schedule with its own object gate; the packed-Q31 layout takes the same real-spectrum schedule at real extents 8 through 64 (the frozen twiddle-table ceiling), mapping real i32 tensors to compact i64 Hermitian bins under the re-frozen raw-high stage policy with saturating conjugation at the 32-bit rails, and its forward/inverse/composition object gates run the unrolled, fft-loops, and Vector-batched pipelines against one independent reference with no i128 carrier; `.ox` exposes static power-of-two `rfft` profiles for real extents 8 through 64 and the five-/nine-bin `irfft` profiles as typed unary expressions and permits the bounded `irfft(rfft(input))` composition; the Q31 real-spectrum profile carries the same source spellings over `tensor[q31,N]`/`tensor[complex_q31,N/2+1]` at real extents 8 through 64 in both directions, emitting the frozen raw-high policy with an object gate on the composed round trip; the interleaved f32 profile carries the same two real spellings, mapping `tensor<Nxf32>` to `tensor<(N+2)xf32>` and back for N in [8, 1024] under the contract `ondrix.cfft` describes, with `.ox` bindings over `tensor[f32,N]`/`tensor[complex_f32,N/2+1]`; real packing/reconstruction remains scalar, while half-size real-pair recombination, function-library, and target consumers remain unsupported |
| Half-size Q31 real split | Experimental `ondrix.rfft_split` is a third, deliberately different real-spectrum contract: it consumes the natural-order output of a half-size packed-Q31 complex FFT over matching `tensor<Nxi64>` values for power-of-two `N` in [4, 32] and emits exactly `N` compact real bins, dropping the Nyquist bin by contract rather than the `N/2+1` Hermitian bins of `ondrix.rfft` or the Q15 whole-transform schedule of `ondrix.rfft_radix4_split`; each mirrored pair spends one saturating floor halving (attribute-identical to the target's scaled saturating subtract, and provably inert because a halved 33-bit difference always fits i32) while every other rounding decision is toward zero, the four twiddle products are exact i64 multiplies scaled afterwards by 2^30, the reachable two's-complement wrap inside the component halvings is contract rather than undefined behavior, and the final combines truncate to i32 with no clamp because the frozen Q2.30 circle bounds every combine strictly below `(1 + sqrt(5)) * 2^30`; twiddles are the frozen `sin`/`cos(pi*k/32)` quadrant that smaller extents stride by `32/N`, and generic scalar lowering reaches object+C differential execution at N=8/16 against an independent reference that also re-derives the inert clamp and the combine bound; the inverse direction, other extents, layouts, and numeric profiles fail closed |
| Spectral magnitude | Experimental `ondrix.cx_magnitude` maps packed Q15 Hermitian bins to Q15 magnitudes through one exact i64 sum of squares and one explicit rounding boundary; `ondsp.sqrt_fixed` defines the all-integer square root (floor or nearest, no reachable ties, saturation at 32767, deterministic zero clamp of out-of-domain input) with a default bit-by-bit scalar lowering and an opt-in IEEE estimate-plus-correction lowering that stays bit-identical; both reach object+C differential execution behind an RFFT64 producer against an independent correction-looped reference; the `forward-ondrix-insert-extract` pass (run by the canonical pipeline, standalone for ablation) forwards constant-index tensor reads through the producer's insert chain (pure SSA value identity — a write with a non-constant index blocks the walk), eliminating the materialized intermediate spectrum between `rfft` and `magnitude` entirely while preserving every numeric boundary, gated by the same differential object harness on the forwarded pipeline; a packed-Q31 profile maps `tensor<Nxi64>` bins to Q31 magnitudes and adds one boundary the Q15 width does not have — two Q1.31 squares already reach `2^63`, so each component is requantized by one bit under a declared `input_rounding` before squaring and the sum is restored by two bits before the root; the restore is clamped at the i64 maximum because no pre-shift avoids the overshoot (magnitude, unlike rms, never divides the sum), and the clamp is inert since the smallest sum it bites already exports a saturated magnitude — the full-scale corner `re = im = INT32_MIN` is a named object witness, `ondsp.sqrt_fixed` gained a matching i32 result profile, and `.ox` binds both boundaries as per-call-site `input_rounding=`/`root_rounding=` parameters; Vector profiles fail closed |
| Fixed DCT-II | Experimental `ondrix.dct` fixes the direct matrix-form contract for extents 4 through 64: tie-guarded compile-time cosine tables, exact i64 row reductions, and one round-half-even boundary per output to the declared `frac = 14 - log2(N)` reading with provably unreachable saturation; generic scalar lowering and object+C differential execution are covered at 8, 32, and 64; a direct bufferization consumer re-expresses every row as a zero-seeded SATURATING i40 `ondsp.reduce_mac` over constant per-row coefficient globals, so the reduction takes the constant-coefficient prefix-range-proof vector route — the complementary legality mechanism to the wrap route matmul and rms use — with an emitted, separately rederived and replayed audit trace, an identity widening export plus the authority round_shift boundary, and its own object+C differential gate with host AVX2 codegen evidence at 8, 32, and the maximal 64-point profile whose DC row realizes the tightest certified prefix bound (2^36 - 2^21 against the i40 rail); the order-preserving output batching takes the unrolled rows as well, one output per lane with the table columns read as immediates, the shared input one element per term, and the export and round_shift applied elementwise, where every row's rail certificate lets the lanes wrap and the narrowest certified i32 term group across the lanes is used; the frontend-compiled `.ox` fixture reaches the same proof-gated route and an executed differential gate of its own, closing the source-to-object chain; an f32 profile derives the same table in binary32 with no tie guard, because the guard certifies a quantized table against an independently specified value while here the binary32 rounding IS the declared constant and the differential reference derives it the same way, and takes the tensor lowering rather than the Q15 coefficient-global route, gated bit-exactly under off and fma against a reference that derives its own table; a Q31 profile moves the boundary onto the product exactly as matmul does — each term takes the derived shift `p = max(0, 2*(W-1) + floor(log2 N) - 62)` under a declared `product_rounding`, required where the boundary exists and refused where it does not, so the export shift is a constant 32 at Q31 against `16 + m` at Q15 and the declared reading is `W - 2 - m` at either width; its coefficient guard range-reduces the angle by `(2n+1)k mod 4N` (exact, changing no emitted Q15, Q31 or f32 coefficient) and takes its own `2^-13` LSB bound; and it takes the scalar tensor lowering rather than the reduce_mac bufferization, whose interface has no vocabulary for a per-term derived narrowing — the same reason the Q31 matmul and rms profiles do; FFT-decomposed forms and target consumers remain unsupported |
| Moving average | Experimental `ondrix.moving_average` defines the valid-boundary mean for any static window in [2, 64] as one exact i64 window sum and one nearest-even division boundary: `ondsp.round_shift` for a power-of-two window (that profile's lowering is unchanged) and the `ondsp.round_div` primitive otherwise, the two agreeing bit-exactly where both exist; the default lowering materializes independent windows and an opt-in sliding-window-reuse mode lowers an incremental exact running sum authorized by the contract; both reach object+C differential execution against one independent reference whose K=3 directed input also witnesses that the equal-tap Q15 FIR reformulation is a different program; an f32 profile recomputes each window as the declared reduction followed by one division and declines the sliding-window reuse, because the incremental form changes the event graph and only the exactness of the integer window sums made that reuse value-neutral; a Q31 profile sums at most 64 Q1.31 values, below `2^38` and exact in i64, and a mean of Q1.(W-1) values stays inside the format, so neither mean spelling gains a boundary at the wider width |
| Fixed-point phase | Experimental `ondrix.cx_phase` maps packed Q15 or Q31 complex elements to their argument expressed in the unsigned turn `ondrix.sine` reads, Q0.16 for the Q15 sine or Q0.32 for the Q31 sine, declared on its own and independently of the component width, so `sine(cx_phase(z))` composes with no conversion at either width and phase arithmetic stays exact under two's-complement wraparound. Two rounding boundaries per element under the one admitted tie rule — the octant ratio division and the table interpolation against a tie-guarded 129-entry arctangent table ending on the exact eighth turn — and everything after them is exact turn arithmetic, which is why the eight octants meet exactly at the axes and diagonals rather than nearly. The origin's phase is the declared value 0. Unlike the 2^16-domain transcendentals this contract is NOT gated exhaustively (the domain is 2^32 pairs) and the gate records what it covers instead: the axes and diagonals over their whole range, one component swept whole against ten pivots of the other, a deterministic random sweep, and the fifteen angles the contract names directly, plus ten named angles at Q31; the components enter only through the ratio, so the Q31 components widen only the absolute values and the ratio numerator (one bit wider than the component) while the Q0.16 turn keeps its 129-entry table; the Q0.32 turn is a different construction rather than a wider table — a 129-point table read by linear interpolation sits about `2^-16` from the real arctangent at any storage width — namely the ratio carried to `2^-32` and the exact angle addition `atan(x) = atan(r_k) + atan(u)`, `u = (x - r_k)/(1 + x r_k)` below `2^-10` from a second division, `atan(u) ~ u - u^3/3`, over a 1025-entry table at `2^33` ending on the exact eighth turn, with the same two nearest-even boundaries (the ratio and the return to Q0.32); over 2^30 pseudo-random Q31 pairs it is exact at 85.6% and one Q0.32 LSB from the correctly rounded turn at the rest, never more, a bound the gate enforces against a `long double` reference alongside bit-exactness against a restatement, and the axes and diagonals stay exact; `.ox` binds `phase` over `complex_q15` or `complex_q31` operands with the turn width as the one call-site choice, `turn=q15` (the default) or `turn=q31`; deviation from the exact argument is measured at about one turn unit and bounded, a weaker evidence class than exhaustive and recorded as such |
| Fixed-point transcendentals | Experimental `ondrix.log2` and `ondrix.exp2` are inverses of each other and are exact contracts rather than approximations: each names a tie-guarded 129-entry compile-time table and one declared interpolation, so its result is a bit-determined function of its input and the distance to the mathematical function is a documented property rather than a looseness. They carry two readings, not one — `log2` takes the unsigned `Q0.16` magnitude and returns signed `Q5.11`, `exp2` the reverse — and each reading is declared in its own numeric attribute rather than projected onto Q15, because a `frac` is always the value's reading. `log2` has one rounding boundary and names `-32768` as the declared value at the pole; `exp2` has two, the mantissa interpolation and a binade placement whose shift amount depends on the input, and the contract keeps them separate rather than folding them. Both 2^16 domains are gated exhaustively against a reference that regenerates the table from its definition, and the round trip is measured against a derived bound rather than asserted to be the identity, which it is not; the Q31 profiles read the unsigned Q0.32 magnitude against the signed Q6.26 exponent (`[-32, 0]`, the pole at `-2^31`) and are new constructions rather than wider tables, since a 129-point table read by linear interpolation is about `2^-16` from either function at any storage width: `log2` is the exponent plus a 1024-point Q30 table plus `log2(1 + r) ~ (r - r^2/2)/ln 2` for the residual ratio one 64-bit division supplies, with ONE rounding boundary and no saturation; `exp2` is a 1024-point table of `2^(k/1024)` at `2^31` read back unsigned plus the third-order series in `x ln 2`, with the binade placement taken on the `2^62`-scaled mantissa so the element has ONE boundary where the Q15 profile needs two; measured over their WHOLE domains (2^32 - 1 nonzero magnitudes, 2^31 in-range exponents) `log2` is exact at 98.0% and `exp2` at 98.5% and neither is ever more than one LSB from the correctly rounded value, the gate enforcing that bound against a `long double` reference alongside bit-exactness against a restatement, and the Q31 round trip is gated as a bound the same way; `.ox` binds both at `q31` through the same projection |
| Elementwise family | Experimental `ondrix.add`, `sub`, `mult`, `abs`, `negate`, `offset`, and `shift` map static rank-1 Q1.15 or Q1.31 tensors elementwise, the exact body running in a carrier twice the storage (i32 for Q15, i64 for Q31) because the product needs 2W bits and negating the storage minimum is part of taking an absolute value; `offset`'s bias and `shift`'s amount are raw values in the declared format, so their ranges move with the width and the same integer names a different real number at the two widths; the Q31 object gate leads with the three inputs an i32 body gets WRONG — `mult(INT32_MIN, INT32_MIN)`, `abs(INT32_MIN)`, `shift_left(INT32_MAX)` — and pinning the carrier at i32 fails it while the Q15 gate stays green. Each states the same two things: what the exact result is over the integers, and what its one declared boundary does to it. The content is not uniform and the contracts do not pretend it is — `mult` carries a full `2(W-1)`-bit requantization under any of the four declared tie rules; a right `shift` carries the same boundary and is where the four rules are furthest apart; `abs` and `negate` carry exactly one asymmetry, the `-32768` input whose result is unrepresentable; `add`, `sub`, and `offset` carry an overflow policy only. A left `shift` still declares a tie rule even though the amount makes it vacuous, so changing the amount cannot silently change which rule applies. The family is fixed point only: an elementwise IEEE operation has no boundary, so an f32 profile would add surface without adding contract. Every unary member is gated over its whole 2^16 input domain and the binary ones over the rails, the reachable halfway products, and a deterministic sweep; the gate also asserts that the four tie rules of a right shift by one give four different answers on 1, 3, -1, -3 |
| Elementwise chain fusion | Opt-in `fuse-ondrix-elementwise-chains` collapses a two-operation chain of unary Q1.15 elementwise members into one operation, or into the input when the pair is the identity, under a certificate that evaluates both programs on all 65536 possible i16 inputs. Every candidate is a real-arithmetic identity and none is unconditionally a contract identity, so the verdicts split on a declared value rather than on the operation: negation is an involution under `wrap` and not under `saturate`; a bias cascade collapses under `wrap` and not under `saturate`; a right-shift cascade collapses under either directed rule and in general under neither nearest rule — with one certified exception, `nearest_even` at amounts -1,-1, which is exactly the kind of fact a per-candidate certificate finds and a per-rule verdict would have missed; this is the double-rounding structure the round_shift cascade study describes reached from the source language. A binary member is refused structurally rather than by a failed certificate: `mult` has a 2^32 domain with neither operand known, so the enumeration cannot be run — the reason the constant-operand `gain` has a merge pass and `mult` has none. The object gate checks both halves, that the emitted objects agree wherever the certificate accepted and that they diverge wherever it refused, reporting the witness input for each refusal |
| Source-level composition | The composable set — the FFT family, `magnitude`, a `fir_filter` input stage, and the elementwise family — is closed under nesting in `.ox` at either uniform fixed width, with one operand grammar and one checker that carries element type and extent from operand to result, so adding a member adds no nesting rule. This is the surface the transformation-legality work applies to: a `mult` cascade has no merged form at all, because neither operand is a compile-time constant and the exhaustive certificate that decides `ondrix.gain` merging has nothing to evaluate |
| CIC decimation | Experimental `ondrix.cic_decimate` names the multiplier-free cascade: `S` integrators in `[1, 8]` at the input rate, a power-of-two rate change `R` in `[2, 4096]`, and `S` combs of differential delay `M` in `{1, 2}`, all carried at the derived register width `W + S*log2(R*M)` bounded at 64 bits — so the growth budget is 48 bits at Q15 and 32 at Q31, and the Q31 profile refuses 33 of the 192 admissible `(S, R, M)` combinations, a four-stage cascade at `R = 512` among them, a refusal that is the contract because carrying the cascade past i64 would make the operation generic-scalar-only — with the exact `2^G` transfer gain removed by one declared requantization at the end. It is the operation whose correctness selects a contract value rather than observing one: every integrator has an unbounded impulse response, so `wrap` is what makes the cascade exact and `saturate` is simply a different, wrong program once an integrator reaches the rail. The mode therefore has no source default. The `ondsp.add_shift`/`ondsp.sub_shift` boundary pair carries the choice and gains its first producer and lowering here. Object gates run both modes against one independent reference (including a wrap gate at the derived-width ceiling W = 64, whose sustained full-scale corpus provably drives the exact combine past i64 — the harness counts the rail crossings and refuses a corpus that stops reaching them), assert they diverge on a full-scale corpus so the mode gate cannot pass by coincidence, and check the steady-state identity that a constant input reappears unchanged under `wrap` while every integrator is far outside the carrier range |
| Rounded division | Experimental `ondsp.round_div` divides a signed value by a static positive divisor under a complete declared policy: exact pre-scale into an input-width + pre-shift carrier (verifier-bounded at 128 bits), Euclidean floor quotient, all four declared rounding modes stated over the non-negative remainder against `divisor - r` (no doubled intermediate exists anywhere), and wrap or saturating narrowing; an odd divisor has no reachable tie so both nearest modes coincide there by construction; exhaustive per-config i16 object gates (all 65536 inputs per divisor x mode), a bit-exact power-of-two agreement gate against `ondsp.round_shift`, directed rail/wrap/tie-quadrant i32 cases, and a pre-scaled i112-carrier profile against an independent __int128 reference; a runtime divisor is a different operation with an explicit zero policy and remains unsupported |
| FIR design generation | Opt-in compile-time evaluation of `ondrix.window_hamming`, `ondrix.window_hann`, `ondrix.window_blackman`, `ondrix.window_kaiser` (rational beta in (0, 50], power-series Bessel evaluation under the documented binary64 budget), and `ondrix.fir_design_windowed_sinc` (lowpass/highpass) into signed Q1.15 or Q1.31 `arith.constant` tables under a fail-closed quantization tie guard (the declared width selects the quantization boundary and nothing else; Q31 takes its own `2^-13` LSB guard, which refuses 20.1% of Hamming extents, 15.9% of Hann, 22.4% of Blackman and 1.2-5.1% of Kaiser against 0.20/0.17/0.07% at Q15, none below extent 225, and the windowed sinc range-reduces its sine argument exactly so its Q31 table matches a 50-digit evaluation coefficient for coefficient) with declared, counted saturation and provenance attributes; the generated constants feed the existing constant-coefficient proof and Vector machinery unchanged; the windowed-sinc designs reach design-to-object execution gates against independently derived golden taps, while the window-only tables are pinned by evaluator goldens, the replayable margin script, and `.ox`-sourced Hamming design-to-object gates at both widths whose references derive the window from its real-valued definition |
| RMS | Experimental `ondrix.rms` reduces a power-of-two-length Q1.15 tensor through the exact i64 sum of squares and exactly two boundaries — the nearest-even mean and the integer square root of the structurally nonnegative mean (the second `ondsp.sqrt_fixed` producer); object+C differential execution covers the zero, full-scale, saturating all-minimum, and exact-square corners; a direct bufferization consumer re-expresses the same contract as one zero-seeded i64 wrapping `ondsp.reduce_mac` proven never to wrap (sum at most 2^42) with an identity raw export followed by the declared arithmetic mean `round_shift`, inherits the exact-modulo horizontal Vector reduction without constant-coefficient proofs under identity-layout function boundaries (other layouts fall back to the ordered scalar reduction), and reaches its own object+C differential gate with host AVX2 codegen evidence up to the 4096-point extent; the frontend-compiled `.ox` fixtures for both declared root roundings reach the same wrap route and an executed differential gate whose directed witness forces the two roundings to disagree, closing the source-to-object chain including the per-call-site rounding parameter; an f32 profile takes any extent in [2, 4096] — the power-of-two requirement exists only so the fixed-point mean is a shift — and carries no rounding attribute: only the reduction is contract indexed, while the mean's division by a representable constant and the correctly rounded square root are the same single unflagged events under off, fma, and fast, gated bit-exactly against an independent reference on a corpus that forces off and fma apart; a Q31 profile carries the same two boundaries plus a third the width forces — an exact sum of N squares of Q1.31 minimums overflows i64 at every admitted extent, so each input is requantized by the derived `k = ceil(log2(N)/2)` under a declared `input_rounding` and the mean is restored by `2k` BEFORE the root rather than after (the two forms differ on 216 of 256 directed trials at N=64), gated at both roundings against an independent reference, and it now bufferizes to the same `ondsp.reduce_mac` as Q15 by applying that pre-shift once per element into a scratch copy the reduction squares exactly, so the exact-modulo horizontal reduction takes the sum of squares while the schedule stage's `vectorize-ondsp-fixed-elementwise-loops` pass takes the requantization copy — an elementwise map whose independent iterations need no range, overflow, or prefix proof, refusing aliased sequences the way the other batchers do — with its own object+C differential through the bufferized pipeline |
| Goertzel | Experimental `ondrix.goertzel` fixes the quantized single-bin energy recursion: one tie-guarded compile-time cosine coefficient (inadmissible bins fail closed), one product boundary and one saturating combine per sample, and an exact i64 energy expression with no output rounding; power-of-two and non-power-of-two lengths and the saturated DC coefficient reach object+C differential execution against an independent per-step reference; the f32 profile keeps the recursion with every boundary removed and indexes only the site that is actually a multiply-add — the coefficient product and its input addition, the state subtraction and the closing energy being single IEEE operations in every mode — which bounds the fast legal set to the two exact contracts' graphs; the lowering selects the fused one and emits it carrying no permission, so the gate pins that chosen graph bitwise rather than testing membership, and a permission check on the translated module keeps the choice from reaching codegen (a `reassoc` left on an explicit fused event is consumed by the pinned backend, which de-fuses it and picks the other member); both profiles evaluate the quarter-turn angles exactly instead of through libm, so bin N/4 has the zero coefficient it names; a Q31 profile spells the step product as `c*s1 / 2^(W-2)` — the same rational as the doubled Q15 form, which stays inside i64 where the doubled one reaches `2^63`; Q15 keeps its spelling, emitted code byte-identical — and takes ONE declared bit on each of `s1`, `s2` and `m` under a `state_rounding` required exactly at Q31 and refused at Q15, since three terms of `2^62` leave i64, so its energy reading is two fractional bits coarser; its Q31 coefficient guard is the first in the project that fires, refusing 1017 of 4198399 admissible `(k, N)` bins per bin, none at `N <= 64`; `.ox` binds the f32 profile only, since no source type names the `tensor<1xi64>` fixed energy, and its two contracts share the dialect gate's reference |
| Fixed-point sine/cosine | Experimental `ondrix.sine`/`ondrix.cosine` map Q15 turn phases (two's-complement wraparound IS phase wraparound) through a tie-guarded 256-entry table with one Q8 nearest-even interpolation boundary and one declared saturating combine per element; the 65536-phase input domain is finite, so the object gate is exhaustive for both operations against an independent mpmath-derived table, including the whole-domain quarter-turn identity between them; the Q31 profile is a different construction rather than a wider table — a 256-entry table read by linear interpolation sits about `2^-15.7` from the real sine at any storage width, and a Q0.16 phase caps the value near `2^-14` because the derivative is `2*pi`, so phase and value are one declared W — third-order angle addition over a 1024-entry table (one table serving both terms, the cosine of a table angle being the sine a quarter turn on), carried at `2^-62` and returned through the SAME two boundaries, measured offline over all `2^32` phases at exact 3022581719, one LSB 1272384953, two LSB 624 and never more, a bound the gate enforces against libm alongside bit-exactness against an independent restatement of the declared algorithm; dropping the third-order term fails the Q31 gate and leaves the Q15 gate green |
| Matrix multiply | Experimental `ondrix.matmul` extends the FIR dot-product contract shape to rank-2 results: exact i64 K-sums and one nearest-even saturating boundary per output element for static `MxK x KxN` shapes with dimensions in [1, 64]; square and rectangular profiles, the non-identity Q15 "identity", and the all-saturating corner reach object+C differential execution; a direct bufferization consumer packs the right operand transposed once (pure data movement) and re-expresses every K-sum as a zero-seeded i40 wrapping `ondsp.reduce_mac` proven never to wrap (sum at most 2^36), so runtime operands inherit the exact-modulo horizontal Vector reduction without constant-coefficient proofs under identity-layout function boundaries (other layouts fall back to the ordered scalar reduction), gated by its own object+C differential and host AVX2 codegen evidence; the order-preserving output batching now takes that column loop as well, loading contiguous row segments of the original right operand and broadcasting the left row element per term, so no packed copy is read and none is kept when every column is batched, with a column count below the lane width taken as one block of the next power-of-two width whose surplus lanes are loaded from the following row while in bounds, zero afterwards, and never stored; the frontend-compiled `.ox` fixture reaches the same wrap route and an executed differential gate of its own, closing the source-to-object chain; an f32 profile over the same extent envelope carries a contract instead of a rounding attribute, because a floating-point element has no requantization boundary after its reduction — it bufferizes without the pack, as a column-tile loop nest whose accumulator runs over the inner axis, so the order-preserving output batching takes its column axis under every contract and no reduction reaches the horizontal route — and it is gated bit-exactly under off and fma against an independent reference, together with the `.ox` binding of the same contract; a Q31 profile adds one boundary on the PRODUCT rather than the inputs, because narrowing an operand before the multiply scales its error by the other operand's magnitude (measured at K=64: about `2^27` worse) — each term is requantized by the derived smallest shift `p = max(0, 2*(W-1) + floor(log2 K) - 62)` under a declared `product_rounding` and the export absorbs what it took, with `p = 0` at every Q15 shape and at `K = 1` so the attribute is required exactly where the boundary exists, and that profile now bufferizes to the same `ondsp.reduce_mac` as Q15, carrying `p` as an `#ondsp.product<full, shift, rounding>` requantized term into a zero-seeded i64 wrapping accumulator at frac `62 - p` proven never to wrap (`K <= 2^(p+1) - 1` terms each at most `2^(62 - p)`), so the order-preserving output batching and the exact-modulo horizontal reduction take it unchanged and its own object+C differential runs through the bufferized pipeline |
| LMS adaptive filter | Experimental `ondrix.lms` fixes the quantized-state least-mean-squares recursion: per sample one exact i64 tap accumulation with a nearest-even output boundary, one saturating error, one quantized step-size product, and one quantized update per tap, with the adapted weights feeding the next sample; the schedule stage's `vectorize-ondsp-fixed-elementwise-updates` pass batches W of the steady-state weight updates into vector lanes, each lane carrying its own state element through the identical declared update (order preserving, so no range or overflow proof; the same statically-refused storage-disjointness obligation and caller-side ABI residual as the fixed decimate batching), reversing the loaded lane order onto the backward sample walk and leaving the guarded prehistory and any partial block on the ordered loop; the schedule stage's `vectorize-ondsp-fixed-window-mac-reduce` pass folds the steady-state tap reduction across the same lanes, a shape no `reduce_mac` route can express because its two operands walk in opposite directions, and one whose accumulate reassociates with no range obligation at all since two's-complement addition is associative and commutative — leaving only the requirement that the batched product carrier hold the exact product; loop-form lowering reaches object+C differential execution where the error signal and final weights must match an independent per-step reference bit-exactly, and the gate pins the compounding witness that fusing the two-stage update quantization into one rounding — a valid real-arithmetic reassociation — first diverges at sample 4 and spreads to most of the error signal through the feedback; the f32 profile keeps the same recursion with every boundary removed and two contract-indexed sites (the tap reduction and each weight update), the error and step scaling being single IEEE operations in every mode, and reaches its own object gate through both textual MLIR and the `.ox` binding; a Q31 profile changes exactly one of the five sites — the K-term tap sum, where two Q1.31 products leave i64 — requantizing each tap product by the same derived `p = floor(log2 K)` under a declared `product_rounding`, the step and update products each being a single product that keeps the full shift |
| Constant gain and certified merging | Experimental `ondrix.gain` fixes one exact Q1.(W-1) constant product at Q15 or Q31 (the raw constant is read at the declared width, so the same integer names a different real number at the two widths) and one declared requantization per element under either of two admissible tie rules — `nearest_even` (ties to even, no fixed one-sided tie direction) or `nearest_ties_positive` (ties toward positive infinity, the add-half hardware convention; the two nearest rules diverge exactly on ties whose floor quotient is even, on both signs — e.g. gain 3 on raw -16384 yields -2 versus -1, and a positive half-tie above an even quotient rounds down versus up); the opt-in `merge-ondrix-gain-cascades` pass rewrites a two-gain cascade into the quantized constant product only under an exhaustive compile-time certificate that evaluates both programs on all 65536 possible inputs UNDER THE DECLARED TIE RULE, records provenance, and leaves uncertified cascades untouched; the certified-mergeable constant set genuinely differs between the rules in both directions (pinned witnesses: a pair certified only under nearest_even, one only under nearest_ties_positive, one under both), so rounding policy parameterizes rewrite legality; object gates replay the certificates over the whole input domain and pin the divergence counts and goldens; the opt-in `fuse-ondrix-gain-into-fir` pass extends the same doctrine to the classic gain-into-constant-taps folding with a per-tap exhaustive term-level certificate and proves the certified set is exactly the trivial one (zero gain or zero tap — no nonzero pair certifies under either rule, structurally: no representable Q15 gain is an exact linear map), with an object-level counterexample gate pinning that even plain negation (gain -32768, negated taps) diverges at exactly the one saturating input, and a companion pair whose terms diverge while every exported output agrees — the reason certificates compare exact accumulator terms, never exported samples; both certificate passes are Q15-only by construction — the Q31 sweep would be `2^32` inputs per candidate pair, not a compile-time check — so Q31 gain works and its cascades never merge; the f32 profile scales by a declared binary32 constant with one multiply per element and no boundary, which makes it the one operation whose three contract declarations denote the same event graph — the three objects agree bit for bit and the emitted multiply carries no permission, because there is none to spend — and both certificate passes refuse it, since an exhaustive Q15 sweep has no floating-point analogue |
| Finite-precision legality gates | Committed object-level counterexample gates document real-arithmetic rewrites that the quantized contracts reject: magnitude fusion across explicit Q1.15 requantization boundaries and Parseval energy substitution through the staged RFFT and magnitude pipeline, each checked bit-exactly with pinned diverging witnesses |
| Object generation and C execution | Implemented for scalar and fixed-width Vector Q15/Q31 FIR-sample/full-output paths, factor-two Q15 and f32 FIR decimation including their `.ox` bindings, factor-two Q15 and f32 scalar FIR interpolation including their `.ox` bindings, valid Q15/f32 convolution/correlation, ordered scalar Q15/Q31/f32 streaming chunks, opt-in materialized Q15/Q31 streaming Vector reuse, the experimental f32 TDF-II plus uniform-Q fixed DF-II SOS cascades, the closed packed-Q15 scalar/Vector butterfly, scalar/Vector forward/inverse CFFT profiles gated at 4/8/64 points, scalar/Vector RFFT/IRFFT profiles gated at 8/16/64/256 points plus the 32-point radix-4 split schedule, the RFFT64 magnitude-spectrum pipeline under both sqrt lowerings, fixed DCT-II at 8/32 points, the moving-average default and sliding-reuse lowerings, generated FIR design tables, the finite-precision legality counterexample gates, the f32 gain and lms contracts, and the `.ox` Q15/Q31/f32 dot/FIR, Q15 streaming FIR/fixed SOS, f32 TDF-II SOS, f32 goertzel, Hamming-window-designed FIR, multi-use-binding elementwise, packed-Q15 CFFT/ICFFT, and packed-Q15/Q31 RFFT/IRFFT source slices, including constexpr fixed FIR specialization |
| Public OrtumCore emulation | Signed i40/frac30 saturating accumulator init, Q15 full-product MAC add/sub, the dual-lane MAC step (exactly two independent per-lane updates), the shifted saturating readout, bit-reversed add/sub addressing, and the packed complex pair `cx_mul_conj`/`cx_bfly` (exact i33 conjugate cross-products and i17 butterflies, per-op shift with `toward_negative` or `nearest_ties_positive` rounding into wrap or saturating i16 components, an `imag_hi`/`real_hi` result layout whose swapped form composes with the cross-sign butterfly variant into exact -+j rotation) implemented through exact Ondsp expansion; every capability carries an object+C differential execution gate, the packed pair against an independent C reference carrying the target-pinned witness card; packed producers and adapter serialization remain unsupported |
| Target export realization | The `ondsp-to-ortumcore` export accepts `toward_negative` directly and `nearest_ties_positive` as a proven composition — the floor readout at shift-1 plus one increment-and-halve, admitted only where the narrower readout provably cannot clip (the nested-floor identity and the saturated-rail argument live in the pass description); a three-way execution gate pins the rail carry and the negative half tie against the scalar authority and the shared independent reference. Opt-in `widen-ondsp-exact-accumulators` re-proves from static loop structure that a wrapping accumulator web can never overflow (updates x 2^30 within the declared width) and retypes it to the i40 saturating target domain, trusting nothing carried by the declaration, so a default-contract program reaches the target path unchanged; the default-contract object gate compiles both twins through this path against the same exact reference. the opt-in `pair-ondsp-fixed-reduction-outputs` pass produces `lanes = 2` webs from pairs of independent single-lane ordered reductions sharing one stream, one output per lane so no sum is ever split across the saturating readout, through three matchers — consecutive webs over private constant tables (interleaved into one aligned pair global), transposed-pack column loops (an interleaved stack pair buffer inside a 1024-byte budget), and sliding-window output loops, the valid-boundary FIR on the bufferized route, whose outputs `n` and `n + 1` share the `K + 1` samples both windows span as the scalar coefficient while the lanes read a tap pair buffer holding tap `j` beside tap `j - 1` with the two out-of-range taps the exact zero, an identity step under every overflow mode — each exact because every lane keeps its own declared update order, refused wherever the deferred store or the buffer budget cannot be proven, an odd last output staying single-lane, and the window shape gated on the generic path by an object+C differential over its three schedules (pair loop plus remainder, unrolled pairs plus remainder, even count); opt-in `convert-ondsp-lane-pairs-to-ortumcore` rewrites function-local `lanes = 2` accumulator webs onto pairs of target accumulators advanced by the dual-lane MAC with per-lane readout compositions, converting a function only after proving every touchpoint convertible so anything else still fails closed downstream; a two-leg execution gate pins both lanes and both rounding compositions against the scalar authority and the shared reference |
| Experimental `.ox` frontend | Standalone Lexer/Parser/AST/Sema/MLIRGen pipeline. A file declares one or more functions, the last of which the module exports; an earlier one is a named body a later function calls, instantiated at the call with its declared contracts copied unchanged, checked once against its own signature, and matched to its arguments there — recursion is unwritable because a callee is visible only after it is declared, and no source-level calling convention is implied. Coverage is rank-1 Q15/Q31/f32 dot, FIR-sample, tensor-value valid/full FIR, static factor-two Q15, Q31 and f32 FIR decimation/interpolation (the Q31 site spells its accumulator triple), valid convolution/correlation, static four-/eight-point packed-Q15 CFFT/ICFFT, static power-of-two 8..64-point packed-Q15 RFFT (five-/nine-bin IRFFT) and the packed-Q31 RFFT/IRFFT real-spectrum pair at the same extents, one packed-Q15 butterfly, dynamic-chunk/static-tap Q15/Q31 streaming FIR, one rank-2 static-section Q15/Q31 fixed DF-II SOS profile, and one Q15/Q31 CIC decimation profile whose state overflow the call site must declare because the language has no defensible default for it; one f32 TDF-II SOS cascade profile admitting only the two exact contracts its operation admits, and the f32 single-bin `goertzel` energy (the Q15 profile has no source spelling because its `tensor<1xi64>` energy names a storage width no source type has); statically bounded Q15 feed-forward reductions may infer a sufficient exact accumulator width, while recursive SOS retains explicit i40 (Q15) or i64 (Q31) update and separate state/output export policies, and every Q31 stateful or resampling site spells its contract because no inferred width makes a 62-bit-product update vacuous; fixed accumulator/export policies, ordered f32 contraction, convolution kernel orientation, chronological streaming state, and the closed `complex_q15` stage-scaling profiles are explicit; a bounded expression AST permits typed unary FFT-family nesting without fusing quantization boundaries; local bindings name intermediate stages and may be read any number of times — each read instantiates a fresh copy of the bound call tree, which the pipeline's canonicalize/cse collapses because the operations are `Pure`, so a reused intermediate such as `add(mult(t, t), t)` is writable while a local no statement reads is a compile error — a `fir_filter` stage may feed the FFT chain under the valid boundary and the explicit executable profile of its width (Q15 or Q31, the design taking the same width), and its coefficients may come from a tensor parameter or from a compile-time design expression (`lowpass`, with odd taps and a strictly proper rational cutoff, or the `hamming`/`hann`/`blackman`/`kaiser` windows with taps in [2, 4096] and Kaiser's rational beta in (0, 50]), legal only in the coefficient slot; source locations reach MLIR, diagnostics are source-aware, and object+C execution is covered; general expression composition, dynamic full boundary/FFT planning, broader resampling, convolution padding/stride/dilation, indexing, loops, multiple kernels, mutable output buffers, broader stateful profiles, and stable source/ABI contracts remain planned |
| Stable public kernel ABI | Planned |

## Canonical Pipeline and Scheduling

`-ondrix-default-pipeline` (and `ondrix-compile --emit=llvm` from `.ox`
source) runs, in order: compile-time design evaluation (the fail-closed
quantization tie guard aborts the compile rather than ship a misquantized
table); contract conversion (operations whose reductions bufferize directly
stay in contract form); proof-gated constant-index forwarding before
bufferization (`forward-ondrix-insert-extract`, described under Spectral
magnitude); boundary bufferization; the automatic schedule stage; and
lowering to the LLVM dialect.

- **Schedule selection.** Inside the schedule stage every candidate
  transform is filtered by its own legality analysis and applied in a
  documented priority order: order-preserving vertical batchings first,
  then saturating-certified and wrap horizontal reductions on the sites
  batching cannot serve. An unauthorized site keeps its ordered scalar
  schedule; the user's only inputs are the numeric contracts in the source.
- **Target vector width.** A pipeline and driver parameter in register bits
  from which each transform's lane count is derived; the same module
  compiles at any width. **Zero is both the all-ordered program and the
  default**, so an undeclared target gets the schedule that is legal
  everywhere rather than a guess; 128 is the NEON/Helium lane count against
  256 for AVX2.
- **Declared accumulator chains.** How many independent partial-sum chains
  the target's multiply-add latency needs covered. Zero, the default,
  derives the count from the declared width the way the measured host
  classes do; a target the width mispredicts declares its own, once, for
  every kernel of that target. Width and latency are different target
  properties, and an in-order core with one vector pipe is where deriving
  one from the other goes wrong.
- **Cost model.** The fixed priority order plus each pass's own
  profitability guards. A measured regret evaluation against the best legal
  candidate is planned for the frozen-revision evaluation. The individual
  pass flags remain available for ablation, testing, and oracle runs.
- **Composition witness.** The composed four-stage `.ox` program
  (windowed-sinc design, valid FIR, RFFT64, magnitude) compiles through
  this pipeline with no hand-picked passes — the evaluated design constants
  feed the certified saturating horizontal reduction — and its
  default-width and ordered width-zero objects are both gated bit-exactly
  against one independent C reference, so schedule invariance of the exact
  contracts is a checked fact.

## Compilation Interfaces

Every knob that can change a produced schedule or code shape belongs to
exactly one of five classes. The canonical compilation path
(`ondrix-compile --emit=llvm`, which runs `-ondrix-default-pipeline`; `--emit=llvmir` additionally translates to LLVM IR and runs LLVM's middle end at `--llvm-opt-level` (default 3) with its loop and SLP vectorizers off, exact on IR that carries no fast-math flag, and the manifest records that level) accepts
only the first two, and exposes no pass option at all: reaching anything in
the fourth class requires dropping to `ondrix-opt` and naming passes.

| Class | Meaning | Members |
| --- | --- | --- |
| Semantic contract | Declared in source; changes the admissible result set | Per-call-site numeric contracts (`accumulator`, `rounding`, `overflow`, `contract=off\|fma\|fast`), FIR `boundary`, algorithm parameters (`factor`, `window`, `gain`, `step_size`, design `taps`/`cutoff`) |
| Target configuration | Describes the machine, not the program | `vector-bits` (register width; lane counts derive from it; zero withdraws the lanes but not the schedule stage — exact contracts get the ordered scalar program, while a `fast` reduction still carries its multi-chain rebuild as scalar chains) |
| Compiler auto decision | Fixed inside the pipeline, not user-facing | Schedule-stage candidate selection, `preserve-bufferizable-reductions`, the constant-reassociation analysis budget, frontend accumulator-width inference |
| Ablation and oracle interface | Forces or measures one alternative; never required to compile | `fft-loops`, `vectorize-static-cfft`, `sliding-window-reuse`, `specialize-canonical-twiddles`, `sqrt-estimate`, `tile-size`, `max-taps`, `record-refusals`, proof-trace emission and replay, individual schedule-pass flags |
| Legacy | Retained for compatibility, outside current claims | `--print-source-locations` |

Three interface kinds are kept distinct in evaluation: an *ablation* forces
one candidate that the legality analysis has proven admissible; an *oracle*
selects the best among all admissible candidates; an *unsafe bypass* applies
a transform without its legality analysis and exists only inside negative
tests. Only the first two produce numbers that describe the compiler.

The default decisions the canonical path makes, recorded here because a
reproduction needs them: FFT lowering uses the unrolled recursive form;
the target vector width is 256 bits; the constant-reassociation analysis
examines at most 64 coefficients; algorithm operations whose reductions
bufferize directly are preserved in contract form until bufferization.

## Supported Q15 Slice

The executable Q15 profile is defined in `fixed-point-semantics.md` and its
evidence state in the table above. The sample, full-output, and
explicit-state streaming forms remain separate algorithm contracts.
Same-boundary FIR, streaming Vector scheduling, Q31 target lowering,
scalable Vector, and a stable C ABI remain outside this supported slice. The
experimental `.ox` Q15 dot and FIR-sample forms are narrow source entries
into the same scalar contract, not a stable language surface.

## Supported Q31 Slice

The executable Q31 profile (full products in i64/frac62 state, raw-high
products in i40/frac30 state) is likewise defined in
`fixed-point-semantics.md`. Raw-high Q31 MACs select the OrtumCore
`q31_mac_add`/`q31_mac_sub` on the shared i40/frac30 accumulator, and a
raw-high reduction exports at frac30 through the same readout: raw-high
accumulation does not implicitly rescale from Q30 to Q31, and the target has
no left-shifting readout.

## Public OrtumCore Emulator

See the "Public OrtumCore emulation" table row; operations without a typed
equation and an executable public consumer fail closed.
