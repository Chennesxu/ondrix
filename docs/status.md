# Implementation Status

Textual MLIR remains the complete development and testing entry point. The
experimental `.ox` frontend is a narrower typed source surface over the same
contracts; its exact coverage is the "Experimental `.ox` frontend" row below.
Bindings expose every declared choice their contract admits; a binding never
narrows a contract silently. Dialect and source contracts may change while
the numeric model is stabilized.

| Capability | Status |
| --- | --- |
| `ondrix`, `ondsp`, and `ortumcore` dialects | Implemented; contracts remain experimental |
| Typed conversion patterns and accumulator type conversion | Implemented |
| Generic scalar lowering | Signed-Q15 full product with signed frac30 accumulator widths of at least 32 bits and signed-Q31 full/raw-high accumulator operations implemented; ordered rank-1 Q15/Q31 memref reductions implemented; from a supported signed frac30 accumulator the export boundary lowers a signed Q15 destination, any signed i32 destination fraction the verifier admits, and the identity signed i64/frac30 reading, while the i64/frac62 profile remains restricted to signed Q31 export; every export is a value-preserving format conversion, and value-changing scalings belong to the declared arithmetic `round_shift`; rank-1 f32 reduction is partial |
| Floating-point contract policy | Every f32 call site names one of three declared evaluation policies. `off` and `fma` are exact contracts, pinned bit-for-bit against independent references; `fast` names a set of two permitted rewrites and is spent by the compiler rather than handed on, so the emitted operations carry no fast-math flags at all. One transform exploits it, `vectorize-ondsp-fp-fast-memref-reduce`, and its schedule is gated at the object level. `docs/f32-contract-evidence.md` is the normative statement of the permissions, the admission rule, and what each route spends; this row reports maturity only and is deliberately not a second copy of it. |
| Canonical pipeline and automatic scheduling | `-ondrix-default-pipeline` (and `ondrix-compile --emit=llvm` from `.ox` source) runs the whole flow with one flag: compile-time design evaluation (the fail-closed quantization tie guard aborts the compile rather than ship a misquantized table), contract conversion (operations whose reductions bufferize directly stay in contract form), proof-gated constant-index forwarding before bufferization (`forward-ondrix-insert-extract`, described under Spectral magnitude), boundary bufferization, an automatic schedule stage, and lowering to the LLVM dialect; inside the schedule stage every candidate transform is filtered by its own legality analysis and applied in a documented priority order (order-preserving vertical batchings first, then saturating-certified and wrap horizontal reductions on the sites batching cannot serve), so an unauthorized site keeps its ordered scalar schedule and the user's only inputs are the numeric contracts in the source; the target vector width is a pipeline and driver parameter in register bits from which each transform's lane count is derived (the same module compiles at any width; zero is both the all-ordered program and the default, so an undeclared target gets the schedule that is legal everywhere rather than a guess, and 128 is the NEON/Helium lane count against 256 for AVX2); the individual pass flags remain available for ablation, testing, and oracle runs; cost selection is the fixed priority order plus each pass's own profitability guards — a measured regret evaluation against the best legal candidate is planned for the frozen-revision evaluation; the composed four-stage `.ox` program (windowed-sinc design, valid FIR, RFFT64, magnitude) compiles through this pipeline with no hand-picked passes — the evaluated design constants feed the certified saturating horizontal reduction — and its default-width and ordered width-zero objects are both gated bit-exactly against one independent C reference, so schedule invariance of the exact contracts is a checked fact |
| Generic Vector CPU lowering | Automatic unit-stride Q15/Q31 chunking, ordered saturating updates, and exact-modulo wrapping reduction implemented; the horizontal wrapping path admits signed frac30 Q15 accumulators wider than i40 up to i64, since exact-modulo reassociation legality is width-independent up to the i64 lane carrier; a genuinely wrapping i48 reduction object gate pins horizontal, ordered, and an exact mod-2^48 reference bit-identical on cases whose ordered prefixes leave the accumulator range, including an exact full-turn return to zero |
| Algorithm transforms | Opt-in specialization for static rank-1 FIR samples using immutable constant memref globals or proven complete unit-stride views: zero-tap elimination, exact-modulo symmetric full-product pairing, and symmetric saturating pairing when complete prefix-range analysis proves both schedules safe |
| Proof audit | Experimental optional JSON traces record constant saturating chunk-reassociation evidence; a read-only replay pass rederives immutable coefficient facts and the subject-bound prefix plan from the original IR, and rejects changes to proof-relevant semantics or trace data; traces are audit artifacts, not legality authority, an independent validator, or a stable certificate format |
| Full-output FIR | Experimental tensor-only destination-style `ondrix.fir_filter` for valid and zero-padded full boundaries; both expose output-axis tiling, with full tiles carrying an explicit global output origin; full lowering uses guarded scalar edges plus a Q15/Q31 Vectorized interior; immutable Q15/Q31 coefficients can activate prefix-proof-authorized horizontal Vector reduction after bufferization; the opt-in `vectorize-ondsp-fp-filter-outputs` pass batches W independent valid-boundary f32 outputs into vector lanes under the exact contracts — each lane runs its declared per-output event graph verbatim (off: separate ordered products and sums; fma: one fused event per tap), so the authorization is structural per-lane event-graph identity, needs no arithmetic law of the element domain, refuses the fast contract (a different relation with its own pending horizontal mode), reads no element the ordered schedule did not, keeps a loop-form tap body valid for any static coefficient count (where unroll-plus-SLP compilers stop at their unroll threshold), and discharges the same statically-refused aliasing obligation as the fixed decimate batching with the `.ox` ABI's declared entry disjointness as the only residual; ordered f32, dynamic shape guards, `K > N`, alias-safe tiled/untiled AOT execution, and direct bufferization are covered; same boundaries, public buffer semantics, fusion, and target-aware selection remain open |
| FIR decimation | Experimental tensor-only `ondrix.fir_decimate` defines valid phase-zero decimation with static factor, increasing tap order, and result length `floor((N-K)/D)+1`; signed-Q15/full with signed frac30 accumulators of at least 32 bits has generic scalar and ordered fixed-Vector lowering; factor-two i40 textual MLIR and portable i34 `.ox` profiles reach object+C differential execution; the opt-in `vectorize-ondsp-fixed-decimate-outputs` pass batches W outputs into a lanes-W accumulator (per-lane tap order unchanged, so the batching needs no arithmetic reassociation proof even for saturating accumulators — the order-preserving complement of the proof-gated horizontal routes — while the memory-schedule disjointness it does require is discharged separately, by static base-storage refusal — with non-entry block arguments, whose incoming operands are in-function facts no caller owns, refused as opaque — plus a documented caller-side ABI residual over function entry arguments and globals only), firing only on the static factor-two identity-layout loop shape whose input, coefficient, and output storage it can see to be disjoint, with an ordered scalar remainder, gated by a differential object test whose i34 profile reaches its saturation rail (8 x 2^30 = 2^33) inside the vector body and, at seventeen taps, exports the clamped trajectory as a non-saturating value the unclamped i40 profile disagrees with; the f32 profile carries the same index relation with the accumulator machinery replaced by its declared contract, reaches direct bufferization, and admits the fast horizontal reduction because a decimated window is still unit stride; reassociating cross-output polyphase transforms, nonzero phase, Q31, and target consumers remain unsupported |
| FIR interpolation | Experimental tensor-only `ondrix.fir_interpolate` defines factor-two phase-zero interpolation as the complete convolution of a zero-inserted input, with result length `(N-1)*2+K` and increasing coefficient update order; signed-Q15/full i40 textual MLIR and portable i33 `.ox` profiles reach generic scalar object+C differential execution, as does the f32 profile under both exact contracts; skipping the inserted-zero terms is a declared f32 event rather than a derivable rewrite (the skipping and materializing programs agree on every finite input and part company at an infinite coefficient, which the object gate witnesses); stateful block forms, polyphase scheduling, Q31, Vector, and target consumers remain unsupported |
| Convolution/correlation | Experimental tensor-only `ondrix.conv1d` preserves distinct valid one-dimensional convolution and correlation identities with explicit kernel orientation and increasing-window update order; Q15 and f32 generic tensor lowering, direct bufferization, dynamic shape guards, LLVM object+C differential execution, unit-stride correlation Vector reuse, and reverse-stride convolution scalar fallback are covered; the `.ox` frontend exposes matching valid tensor forms; full/same padding, stride, dilation, multidimensional/grouped forms, complex conjugation, and target selection remain unsupported |
| Streaming FIR | Experimental tensor-only `ondrix.fir_stream` with explicit chronological `K-1` history; ordered Q15/Q31/f32 generic scalar lowering, empty and short chunks, `K=1`, dynamic shape guards, multi-chunk state equivalence, and object+C execution are covered; an opt-in materialized concat decomposition reuses the existing Q15/Q31 Vector path; `.ox` exposes one dynamic-chunk/static-tap Q15 profile with inferred exact accumulation and two tensor results; zero-copy buffer scheduling, broader source profiles, reset/resampling policies, circular-buffer capabilities, and stable ABI remain open |
| Recursive filters | Experimental tensor-only `ondrix.sos_filter_tdf2` defines an explicit f32 transposed-direct-form-II cascade; experimental `ondrix.sos_filter_df2_fixed` separately defines a signed uniform-Q DF-II cascade for Q15/full+i40/frac30 and Q31/full+i64/frac62 with independent state/output export policies; both have dynamic section guards, split/empty-chunk state equivalence, generic lowering, and object+C differential execution; `.ox` exposes one static-section Q15 DF-II profile with explicit accumulator and state/output policies plus whole/split execution; nonuniform fixed scaling, raw-high products, Vector lowering, public buffer semantics, target selection, broader source profiles, and stable state ABI remain unsupported |
| Packed Q15 butterfly | One closed scalar/fixed-Vector `i32` profile is executable: signed-Q15 complex values use imag-high/real-low packing, exact i33/Q30 cross-products, explicit nearest-even saturating product and output requantization, generic elementwise lowering, and object+C differential execution; broader complex profiles, frontend butterfly syntax, and OrtumCore selection remain unsupported |
| Packed Q15 CFFT | Experimental static power-of-two forward and inverse radix-2 DIT transforms for extents 4 through 1024 over matching `tensor<Nxi32>` values with natural input/output order, recursive even/odd stage decomposition, tie-guard-generated Q15 twiddle tables, explicit per-stage scaling, generic scalar lowering, and object+C differential execution (CI gates at 4/8/64); an opt-in fft-loops conversion mode lowers the same dataflow as bit-reversal and stage loops over in-memory twiddle tables, bit-identical per element and gated against the same references, collapsing the unrolled object footprint and compile cost (RFFT256 object 313 KB to 5 KB); an opt-in conversion mode batches independent combine-stage butterflies into fixed-length Vector arithmetic without crossing stage or requantization boundaries, and agrees bit-for-bit with scalar/reference execution; the two opt-in modes are mutually exclusive; inverse transforms use conjugated twiddles and the per-stage shifts provide `1/N` normalization; an independent opt-in scalar-finalizer mode consumes subject-bound legality plans for exact finite-precision `+1` and `-j` identities while other constants and runtime twiddles retain generic i33 products; the `.ox` frontend exposes the bounded four-/eight-point profiles as `cfft` and `icfft` over `tensor[complex_q15,4|8]` and permits unary CFFT/ICFFT nesting while preserving each intermediate quantization boundary; a packed-Q31 profile (`tensor<Nxi64>`, imag high / real low, extents 4 through 64) reuses the same recursion with exact i128 cross-product carriers — the exactness is required by the `ondsp.cx_butterfly` contract, whose arbitrary SSA twiddles let `br*wi + bi*wr` reach exactly `2^63`, and the operation-level rail gate (scalar and two-lane Vector, all four sign×parity shift-31 tie cells) refutes a WRAPPING i64 carrier at that corner; i128 is the exact generic carrier choice rather than the only legal one, since `2^63` is the single value past i64 and an overflow-aware saturating-i64 sum is observably equivalent under this profile's declared boundary (executed as a mutation experiment: wrapping i64 fails the gate with the predicted component swap, saturating i64 passes the entire corpus) and would need its own equivalence proof; inside the CFFT itself the frozen unit-circle twiddles bound the same sum by `0.7071 * INT64_MAX`, so an i64 carrier is analytically unreachable from wrapping there and the CFFT corpus cannot decide the carrier — one nearest-even shift-31 boundary per product term and the staged per-stage scaling, over frozen 50-digit-mpmath Q31 twiddle tables (in-compiler binary64 evaluation cannot prove correct rounding at the Q31 guard even though the actual margins are large; the generator's CI-run self-check reproduces the pinned Q15 stage-2/4/8 words and both complete extent-64 distinct word sets the compiler emits), with forward/inverse object+C differential gates at 4/8/16/64 against an independent __int128 reference and a bit-exact staged forward∘inverse composition whose nominal net scale is 1/N — not an identity — plus an empirical ±6-LSB consistency check on bounded inputs; Q31 loop-mode/CFFT-Vector-mode conversions, `.ox` Q31 bindings, and target consumers remain unsupported; other expression composition and target execution remain unsupported |
| Packed Q15 RFFT | Experimental static power-of-two `ondrix.rfft`/`ondrix.irfft` profiles for real extents 8 through 1024 map real Q15 tensors to compact natural-order packed Hermitian bins and back; DC/Nyquist imaginary components are canonicalized to zero, inverse reconstruction uses saturating Q15 conjugation, and all full-length CFFT stage boundaries remain observable; generic scalar lowering and an opt-in fixed-Vector mode for independent packed-complex CFFT combine stages both reach LLVM object+C differential execution (CI gates at 8/16/64/256) and sequential finite-precision round trips; the opt-in fft-loops lowering additionally gates the maximum 1024-point extent in CI, which the unrolled form could not afford to compile; a separate static 32-point `ondrix.rfft_radix4_split` contract fixes an explicit radix-4 split schedule with its own object gate; `.ox` exposes static power-of-two `rfft` profiles for real extents 8 through 64 and the five-/nine-bin `irfft` profiles as typed unary expressions and permits the bounded `irfft(rfft(input))` composition; real packing/reconstruction remains scalar, while half-size real-pair recombination, function-library, and target consumers remain unsupported |
| Spectral magnitude | Experimental `ondrix.cx_magnitude` maps packed Q15 Hermitian bins to Q15 magnitudes through one exact i64 sum of squares and one explicit rounding boundary; `ondsp.sqrt_fixed` defines the all-integer square root (floor or nearest, no reachable ties, saturation at 32767, deterministic zero clamp of out-of-domain input) with a default bit-by-bit scalar lowering and an opt-in IEEE estimate-plus-correction lowering that stays bit-identical; both reach object+C differential execution behind an RFFT64 producer against an independent correction-looped reference; the `forward-ondrix-insert-extract` pass (run by the canonical pipeline, standalone for ablation) forwards constant-index tensor reads through the producer's insert chain (pure SSA value identity — a write with a non-constant index blocks the walk), eliminating the materialized intermediate spectrum between `rfft` and `magnitude` entirely while preserving every numeric boundary, gated by the same differential object harness on the forwarded pipeline; Vector and wider-width profiles fail closed |
| Fixed DCT-II | Experimental `ondrix.dct` fixes the direct matrix-form contract for extents 4 through 64: tie-guarded compile-time cosine tables, exact i64 row reductions, and one round-half-even boundary per output to the declared `frac = 14 - log2(N)` reading with provably unreachable saturation; generic scalar lowering and object+C differential execution are covered at 8, 32, and 64; a direct bufferization consumer re-expresses every row as a zero-seeded SATURATING i40 `ondsp.reduce_mac` over constant per-row coefficient globals, so the reduction takes the constant-coefficient prefix-range-proof vector route — the complementary legality mechanism to the wrap route matmul and rms use — with an emitted, separately rederived and replayed audit trace, an identity widening export plus the authority round_shift boundary, and its own object+C differential gate with host AVX2 codegen evidence at 8, 32, and the maximal 64-point profile whose DC row realizes the tightest certified prefix bound (2^36 - 2^21 against the i40 rail); the frontend-compiled `.ox` fixture reaches the same proof-gated route and an executed differential gate of its own, closing the source-to-object chain; an f32 profile derives the same table in binary32 with no tie guard, because the guard certifies a quantized table against an independently specified value while here the binary32 rounding IS the declared constant and the differential reference derives it the same way, and takes the tensor lowering rather than the Q15 coefficient-global route, gated bit-exactly under off and fma against a reference that derives its own table; FFT-decomposed forms and target consumers remain unsupported |
| Moving average | Experimental `ondrix.moving_average` defines the valid-boundary mean for any static window in [2, 64] as one exact i64 window sum and one nearest-even division boundary: `ondsp.round_shift` for a power-of-two window (that profile's lowering is unchanged) and the `ondsp.round_div` primitive otherwise, the two agreeing bit-exactly where both exist; the default lowering materializes independent windows and an opt-in sliding-window-reuse mode lowers an incremental exact running sum authorized by the contract; both reach object+C differential execution against one independent reference whose K=3 directed input also witnesses that the equal-tap Q15 FIR reformulation is a different program; an f32 profile recomputes each window as the declared reduction followed by one division and declines the sliding-window reuse, because the incremental form changes the event graph and only the exactness of the integer window sums made that reuse value-neutral |
| Fixed-point transcendentals | Experimental `ondrix.log2` and `ondrix.exp2` are inverses of each other and are exact contracts rather than approximations: each names a tie-guarded 129-entry compile-time table and one declared interpolation, so its result is a bit-determined function of its input and the distance to the mathematical function is a documented property rather than a looseness. They carry two readings, not one — `log2` takes the unsigned `Q0.16` magnitude and returns signed `Q5.11`, `exp2` the reverse — and each reading is declared in its own numeric attribute rather than projected onto Q15, because a `frac` is always the value's reading. `log2` has one rounding boundary and names `-32768` as the declared value at the pole; `exp2` has two, the mantissa interpolation and a binade placement whose shift amount depends on the input, and the contract keeps them separate rather than folding them. Both 2^16 domains are gated exhaustively against a reference that regenerates the table from its definition, and the round trip is measured against a derived bound rather than asserted to be the identity, which it is not |
| Elementwise family | Experimental `ondrix.add`, `sub`, `mult`, `abs`, `negate`, `offset`, and `shift` map static rank-1 Q1.15 tensors elementwise. Each states the same two things: what the exact result is over the integers, and what its one declared boundary does to it. The content is not uniform and the contracts do not pretend it is — `mult` carries a full Q30 requantization under any of the four declared tie rules; a right `shift` carries the same boundary and is where the four rules are furthest apart; `abs` and `negate` carry exactly one asymmetry, the `-32768` input whose result is unrepresentable; `add`, `sub`, and `offset` carry an overflow policy only. A left `shift` still declares a tie rule even though the amount makes it vacuous, so changing the amount cannot silently change which rule applies. The family is fixed point only: an elementwise IEEE operation has no boundary, so an f32 profile would add surface without adding contract. Every unary member is gated over its whole 2^16 input domain and the binary ones over the rails, the reachable halfway products, and a deterministic sweep; the gate also asserts that the four tie rules of a right shift by one give four different answers on 1, 3, -1, -3 |
| Elementwise chain fusion | Opt-in `fuse-ondrix-elementwise-chains` collapses a two-operation chain of unary Q1.15 elementwise members into one operation, or into the input when the pair is the identity, under a certificate that evaluates both programs on all 65536 possible i16 inputs. Every candidate is a real-arithmetic identity and none is unconditionally a contract identity, so the verdicts split on a declared value rather than on the operation: negation is an involution under `wrap` and not under `saturate`; a bias cascade collapses under `wrap` and not under `saturate`; a right-shift cascade collapses under either directed rule and under neither nearest rule, which is the double-rounding structure the round_shift cascade study describes reached from the source language. A binary member is refused structurally rather than by a failed certificate: `mult` has a 2^32 domain with neither operand known, so the enumeration cannot be run — the reason the constant-operand `gain` has a merge pass and `mult` has none. The object gate checks both halves, that the emitted objects agree wherever the certificate accepted and that they diverge wherever it refused, reporting the witness input for each refusal |
| Source-level composition | The composable set — the FFT family, `magnitude`, a `fir_filter` input stage, and the elementwise family — is closed under nesting in `.ox`, with one operand grammar and one checker that carries element type and extent from operand to result, so adding a member adds no nesting rule. This is the surface the transformation-legality work applies to: a `mult` cascade has no merged form at all, because neither operand is a compile-time constant and the exhaustive certificate that decides `ondrix.gain` merging has nothing to evaluate |
| CIC decimation | Experimental `ondrix.cic_decimate` names the multiplier-free cascade: `S` integrators in `[1, 8]` at the input rate, a power-of-two rate change `R` in `[2, 4096]`, and `S` combs of differential delay `M` in `{1, 2}`, all carried at the derived register width `16 + S*log2(R*M)` bounded at 64 bits, with the exact `2^G` transfer gain removed by one declared requantization at the end. It is the operation whose correctness selects a contract value rather than observing one: every integrator has an unbounded impulse response, so `wrap` is what makes the cascade exact and `saturate` is simply a different, wrong program once an integrator reaches the rail. The mode therefore has no source default. The `ondsp.add_shift`/`ondsp.sub_shift` boundary pair carries the choice and gains its first producer and lowering here. Object gates run both modes against one independent reference, assert they diverge on a full-scale corpus so the mode gate cannot pass by coincidence, and check the steady-state identity that a constant input reappears unchanged under `wrap` while every integrator is far outside the carrier range |
| Rounded division | Experimental `ondsp.round_div` divides a signed value by a static positive divisor under a complete declared policy: exact pre-scale into an input-width + pre-shift carrier (verifier-bounded at 128 bits), Euclidean floor quotient, all four declared rounding modes stated over the non-negative remainder against `divisor - r` (no doubled intermediate exists anywhere), and wrap or saturating narrowing; an odd divisor has no reachable tie so both nearest modes coincide there by construction; exhaustive per-config i16 object gates (all 65536 inputs per divisor x mode), a bit-exact power-of-two agreement gate against `ondsp.round_shift`, directed rail/wrap/tie-quadrant i32 cases, and a pre-scaled i112-carrier profile against an independent __int128 reference; a runtime divisor is a different operation with an explicit zero policy and remains unsupported |
| FIR design generation | Opt-in compile-time evaluation of `ondrix.window_hamming`, `ondrix.window_hann`, `ondrix.window_blackman`, `ondrix.window_kaiser` (rational beta in (0, 50], power-series Bessel evaluation under the documented binary64 budget), and `ondrix.fir_design_windowed_sinc` (lowpass/highpass) into signed Q1.15 `arith.constant` tables under a fail-closed quantization tie guard with declared, counted saturation and provenance attributes; the generated constants feed the existing constant-coefficient proof and Vector machinery unchanged; the windowed-sinc designs reach design-to-object execution gates against independently derived golden taps, while the window-only tables are pinned by evaluator goldens and the replayable margin script |
| RMS | Experimental `ondrix.rms` reduces a power-of-two-length Q1.15 tensor through the exact i64 sum of squares and exactly two boundaries — the nearest-even mean and the integer square root of the structurally nonnegative mean (the second `ondsp.sqrt_fixed` producer); object+C differential execution covers the zero, full-scale, saturating all-minimum, and exact-square corners; a direct bufferization consumer re-expresses the same contract as one zero-seeded i64 wrapping `ondsp.reduce_mac` proven never to wrap (sum at most 2^42) with an identity raw export followed by the declared arithmetic mean `round_shift`, inherits the exact-modulo horizontal Vector reduction without constant-coefficient proofs under identity-layout function boundaries (other layouts fall back to the ordered scalar reduction), and reaches its own object+C differential gate with host AVX2 codegen evidence up to the 4096-point extent; the frontend-compiled `.ox` fixtures for both declared root roundings reach the same wrap route and an executed differential gate whose directed witness forces the two roundings to disagree, closing the source-to-object chain including the per-call-site rounding parameter; an f32 profile takes any extent in [2, 4096] — the power-of-two requirement exists only so the fixed-point mean is a shift — and carries no rounding attribute: only the reduction is contract indexed, while the mean's division by a representable constant and the correctly rounded square root are the same single unflagged events under off, fma, and fast, gated bit-exactly against an independent reference on a corpus that forces off and fma apart |
| Goertzel | Experimental `ondrix.goertzel` fixes the quantized single-bin energy recursion: one tie-guarded compile-time cosine coefficient (inadmissible bins fail closed), one product boundary and one saturating combine per sample, and an exact i64 energy expression with no output rounding; power-of-two and non-power-of-two lengths and the saturated DC coefficient reach object+C differential execution against an independent per-step reference; the f32 profile keeps the recursion with every boundary removed and indexes only the site that is actually a multiply-add — the coefficient product and its input addition, the state subtraction and the closing energy being single IEEE operations in every mode — which bounds the fast legal set to the two exact contracts' graphs; the lowering selects the fused one and emits it carrying no permission, so the gate pins that chosen graph bitwise rather than testing membership, and a permission check on the translated module keeps the choice from reaching codegen (a `reassoc` left on an explicit fused event is consumed by the pinned backend, which de-fuses it and picks the other member); both profiles evaluate the quarter-turn angles exactly instead of through libm, so bin N/4 has the zero coefficient it names |
| Fixed-point sine/cosine | Experimental `ondrix.sine`/`ondrix.cosine` map Q15 turn phases (two's-complement wraparound IS phase wraparound) through a tie-guarded 256-entry table with one Q8 nearest-even interpolation boundary and one declared saturating combine per element; the 65536-phase input domain is finite, so the object gate is exhaustive for both operations against an independent mpmath-derived table, including the whole-domain quarter-turn identity between them |
| Matrix multiply | Experimental `ondrix.matmul` extends the FIR dot-product contract shape to rank-2 results: exact i64 K-sums and one nearest-even saturating boundary per output element for static `MxK x KxN` shapes with dimensions in [1, 64]; square and rectangular profiles, the non-identity Q15 "identity", and the all-saturating corner reach object+C differential execution; a direct bufferization consumer packs the right operand transposed once (pure data movement) and re-expresses every K-sum as a zero-seeded i40 wrapping `ondsp.reduce_mac` proven never to wrap (sum at most 2^36), so runtime operands inherit the exact-modulo horizontal Vector reduction without constant-coefficient proofs under identity-layout function boundaries (other layouts fall back to the ordered scalar reduction), gated by its own object+C differential and host AVX2 codegen evidence; the frontend-compiled `.ox` fixture reaches the same wrap route and an executed differential gate of its own, closing the source-to-object chain; an f32 profile over the same extent envelope carries a contract instead of a rounding attribute, because a floating-point element has no requantization boundary after its reduction — the existing transposed pack keeps both reduction operands unit-stride, so the schedule stage serves them and a `fast` call site reaches the horizontal reduction — and it is gated bit-exactly under off and fma against an independent reference, together with the `.ox` binding of the same contract |
| LMS adaptive filter | Experimental `ondrix.lms` fixes the quantized-state least-mean-squares recursion: per sample one exact i64 tap accumulation with a nearest-even output boundary, one saturating error, one quantized step-size product, and one quantized update per tap, with the adapted weights feeding the next sample; loop-form lowering reaches object+C differential execution where the error signal and final weights must match an independent per-step reference bit-exactly, and the gate pins the compounding witness that fusing the two-stage update quantization into one rounding — a valid real-arithmetic reassociation — first diverges at sample 4 and spreads to most of the error signal through the feedback; the f32 profile keeps the same recursion with every boundary removed and two contract-indexed sites (the tap reduction and each weight update), the error and step scaling being single IEEE operations in every mode, and reaches its own object gate through both textual MLIR and the `.ox` binding |
| Constant gain and certified merging | Experimental `ondrix.gain` fixes one exact Q1.15 constant product and one declared requantization per element under either of two admissible tie rules — `nearest_even` (ties to even, no fixed one-sided tie direction) or `nearest_ties_positive` (ties toward positive infinity, the add-half hardware convention; the two nearest rules diverge exactly on ties whose floor quotient is even, on both signs — e.g. gain 3 on raw -16384 yields -2 versus -1, and a positive half-tie above an even quotient rounds down versus up); the opt-in `merge-ondrix-gain-cascades` pass rewrites a two-gain cascade into the quantized constant product only under an exhaustive compile-time certificate that evaluates both programs on all 65536 possible inputs UNDER THE DECLARED TIE RULE, records provenance, and leaves uncertified cascades untouched; the certified-mergeable constant set genuinely differs between the rules in both directions (pinned witnesses: a pair certified only under nearest_even, one only under nearest_ties_positive, one under both), so rounding policy parameterizes rewrite legality; object gates replay the certificates over the whole input domain and pin the divergence counts and goldens; the opt-in `fuse-ondrix-gain-into-fir` pass extends the same doctrine to the classic gain-into-constant-taps folding with a per-tap exhaustive term-level certificate and proves the certified set is exactly the trivial one (zero gain or zero tap — no nonzero pair certifies under either rule, structurally: no representable Q15 gain is an exact linear map), with an object-level counterexample gate pinning that even plain negation (gain -32768, negated taps) diverges at exactly the one saturating input, and a companion pair whose terms diverge while every exported output agrees — the reason certificates compare exact accumulator terms, never exported samples; the f32 profile scales by a declared binary32 constant with one multiply per element and no boundary, which makes it the one operation whose three contract declarations denote the same event graph — the three objects agree bit for bit and the emitted multiply carries no permission, because there is none to spend — and both certificate passes refuse it, since an exhaustive Q15 sweep has no floating-point analogue |
| Finite-precision legality gates | Committed object-level counterexample gates document real-arithmetic rewrites that the quantized contracts reject: magnitude fusion across explicit Q1.15 requantization boundaries and Parseval energy substitution through the staged RFFT and magnitude pipeline, each checked bit-exactly with pinned diverging witnesses |
| Object generation and C execution | Implemented for scalar and fixed-width Vector Q15/Q31 FIR-sample/full-output paths, factor-two Q15 and f32 FIR decimation including their `.ox` bindings, factor-two Q15 and f32 scalar FIR interpolation including their `.ox` bindings, valid Q15/f32 convolution/correlation, ordered scalar Q15/Q31/f32 streaming chunks, opt-in materialized Q15/Q31 streaming Vector reuse, the experimental f32 TDF-II plus uniform-Q fixed DF-II SOS cascades, the closed packed-Q15 scalar/Vector butterfly, scalar/Vector forward/inverse CFFT profiles gated at 4/8/64 points, scalar/Vector RFFT/IRFFT profiles gated at 8/16/64/256 points plus the 32-point radix-4 split schedule, the RFFT64 magnitude-spectrum pipeline under both sqrt lowerings, fixed DCT-II at 8/32 points, the moving-average default and sliding-reuse lowerings, generated FIR design tables, the finite-precision legality counterexample gates, the f32 gain and lms contracts, and the `.ox` Q15/Q31/f32 dot/FIR, Q15 streaming FIR/fixed SOS, packed-Q15 CFFT/ICFFT, and packed-Q15 RFFT/IRFFT source slices, including constexpr fixed FIR specialization |
| Public OrtumCore emulation | Signed i40/frac30 saturating accumulator init and Q15 full-product MAC add/sub implemented through exact Ondsp expansion |
| Experimental `.ox` frontend | Standalone Lexer/Parser/AST/Sema/MLIRGen pipeline. A file declares one or more functions, the last of which the module exports; an earlier one is a named body a later function calls, instantiated at the call with its declared contracts copied unchanged, checked once against its own signature, and matched to its arguments there — recursion is unwritable because a callee is visible only after it is declared, and no source-level calling convention is implied. Coverage is rank-1 Q15/Q31/f32 dot, FIR-sample, tensor-value valid/full FIR, static factor-two Q15 and f32 FIR decimation/interpolation, valid convolution/correlation, static four-/eight-point packed-Q15 CFFT/ICFFT, static power-of-two 8..64-point packed-Q15 RFFT (five-/nine-bin IRFFT), one packed-Q15 butterfly, dynamic-chunk/static-tap Q15 streaming FIR, one rank-2 static-section Q15 fixed DF-II SOS profile, and one Q15 CIC decimation profile whose state overflow the call site must declare because the language has no defensible default for it; statically bounded Q15 feed-forward reductions may infer a sufficient exact accumulator width, while recursive SOS retains explicit i40 update and separate state/output export policies; fixed accumulator/export policies, ordered f32 contraction, convolution kernel orientation, chronological streaming state, and the closed `complex_q15` stage-scaling profiles are explicit; a bounded expression AST permits typed unary FFT-family nesting without fusing quantization boundaries; single-use local bindings name intermediate stages as pure spelling for the same nested tree (unused or doubly-consumed locals are compile errors), a `fir_filter` stage may feed the FFT chain under the valid boundary and the explicit executable Q15 profile, and its coefficients may come from a tensor parameter or from the compile-time `lowpass` Hamming-windowed-sinc design expression (odd taps, strictly proper rational cutoff), legal only in the coefficient slot; source locations reach MLIR, diagnostics are source-aware, and object+C execution is covered; general expression composition, dynamic full boundary/FFT planning, broader resampling, convolution padding/stride/dilation, indexing, loops, multiple kernels, mutable output buffers, broader stateful profiles, and stable source/ABI contracts remain planned |
| Stable public kernel ABI | Planned |

## Compilation Interfaces

Every knob that can change a produced schedule or code shape belongs to
exactly one of five classes. The canonical compilation path
(`ondrix-compile --emit=llvm`, which runs `-ondrix-default-pipeline`) accepts
only the first two, and exposes no pass option at all: reaching anything in
the fourth class requires dropping to `ondrix-opt` and naming passes.

| Class | Meaning | Members |
| --- | --- | --- |
| Semantic contract | Declared in source; changes the admissible result set | Per-call-site numeric contracts (`accumulator`, `rounding`, `overflow`, `contract=off\|fma\|fast`), FIR `boundary`, algorithm parameters (`factor`, `window`, `gain`, `step_size`, design `taps`/`cutoff`) |
| Target configuration | Describes the machine, not the program | `vector-bits` (register width; lane counts derive from it; zero yields the ordered program) |
| Compiler auto decision | Fixed inside the pipeline, not user-facing | Schedule-stage candidate selection, `preserve-bufferizable-reductions`, the constant-reassociation analysis budget, frontend accumulator-width inference |
| Ablation and oracle interface | Forces or measures one alternative; never required to compile | `fft-loops`, `vectorize-static-cfft`, `sliding-window-reuse`, `specialize-canonical-twiddles`, `sqrt-estimate`, `tile-size`, `max-taps`, `record-refusals`, proof-trace emission and replay, individual schedule-pass flags |
| Legacy | Retained for compatibility, outside current claims | `--print-source-locations` |

Three interface kinds are kept distinct in evaluation: an *ablation* forces
one candidate that the legality analysis has proven admissible; an *oracle*
selects the best among all admissible candidates; an *unsafe bypass* applies
a transform without its legality analysis and exists only inside negative
tests. Only the first two produce numbers that describe the compiler.

The default decisions the canonical path makes, recorded here because a
reproduction needs them: FFT lowering uses the unrolled recursive form;
the target vector width is 256 bits; the constant-reassociation analysis
examines at most 64 coefficients; algorithm operations whose reductions
bufferize directly are preserved in contract form until bufferization.

## Supported Q15 Slice

The executable Q15 profile is defined in `fixed-point-semantics.md` and its
evidence state in the table above. The sample, full-output, and
explicit-state streaming forms remain separate algorithm contracts.
Same-boundary FIR, streaming Vector scheduling, Q31 target lowering,
scalable Vector, and a stable C ABI remain outside this supported slice. The
experimental `.ox` Q15 dot and FIR-sample forms are narrow source entries
into the same scalar contract, not a stable language surface.

## Supported Q31 Slice

The executable Q31 profile (full products in i64/frac62 state, raw-high
products in i40/frac30 state) is likewise defined in
`fixed-point-semantics.md`. Q31 OrtumCore capability selection remains
unsupported. Raw-high accumulation does not implicitly rescale from Q30 to
Q31.

## Public OrtumCore Emulator

See the "Public OrtumCore emulation" table row; operations without a typed
equation and an executable public consumer fail closed.
